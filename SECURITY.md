# 安全政策 · Security Policy

XVPN 是一个处理用户**连接凭据**（WireGuard 私钥、OpenVPN 证书与私钥、Hysteria2
密码等）并决定**全部流量走向**的 VPN 客户端。因此安全报告被认真对待。

## 一、如何报告漏洞

**请使用 GitHub 的私密漏洞报告渠道，不要在公开 issue 里披露细节：**

<https://github.com/LSD-Apps/XVPN/security/advisories/new>

如果该入口不可用，请开一个**不含任何技术细节**的 issue，只说明「有一份安全
报告、需要一个私密渠道」，由维护者先建立私密沟通，再提交细节。

> 本项目当前**没有**公开的安全邮箱，也**没有**第三方安全响应团队（PSIRT）。
> 请不要把报告发往任何自称代表本项目的地址——那可能不是我们。
> 维护者若日后增设专用邮箱，会优先更新本文件。

## 二、我们承诺什么

- 尽快确认收到，并给出初步判断与影响范围（这是个人维护的开源项目，
  **不承诺固定 SLA**，但不会放着不管）；
- 与你协作确定修复与公开披露的时间，修复发布后再公开细节；
- 在你的同意下，于 release notes 或 [`CHANGELOG.md`](CHANGELOG.md) 中致谢
  （你要求匿名则匿名）。

## 三、支持范围

只有**最新发布版本**接受安全修复，旧版本不回补。请先升级到最新版再报告。

| 版本 | 是否支持安全修复 |
| --- | --- |
| 最新 release | ✅ |
| 更早版本 | ❌ |

桌面端（Windows / Linux）与 Android 端均在范围内。

## 四、范围

**在范围内（欢迎报告）：**

- 本项目自身的代码：配置解析、内核配置生成、分流决策、DNS 处理、
  系统代理的接管与还原、凭据存储、Android `VpnService` 集成、更新链路；
- 会导致**凭据泄露**、**流量绕过隧道**、**系统代理残留**、**权限提升**的问题；
- 发布物问题：构建/签名、`SHA256SUMS.txt` 校验和、或 Release 附件被篡改。

**通常不在范围内（仍欢迎告知，但请同时报给对应上游）：**

- **上游内核 sing-box 的漏洞**——请报告给
  <https://github.com/SagerNet/sing-box/security>。本项目会在版本跟进中更新内核，
  同时会在获得修复版本后尽快升级；
- 用户自备服务器 / 节点自身的安全问题；
- 需要设备已被完全攻陷、或需要物理接触才成立的问题；
- 用户自身操作导致的问题（例如把私钥贴到公开位置）；
- 第三方依赖的漏洞（请同时报告给对应上游，我们会跟进升级）。

## 五、加固现状与诚实边界

- **密码学不由本项目实现**：加密与隧道协议全部来自 sing-box。本项目只做配置
  生成与分流决策，这也是它攻击面相对较小的原因。
- **凭据存储**：各平台的具体存储方式（Android 应用私有目录、Windows DPAPI、
  Linux libsecret，以及 libsecret 缺失时降级明文）以
  [`PRIVACY.md`](PRIVACY.md) 的说明为准；**没有的加密我们不会声称有**。
- **没有遥测**：本项目不收集任何使用数据、不集成崩溃上报，因此我们无法依靠
  产品内监控发现漏洞——外部报告是主要来源，这也是本文件存在的原因。
- **签名与校验**：Android 发行包使用固定密钥签名（缺失签名材料时构建直接失败，
  不回退 debug 密钥）；Release 附 `SHA256SUMS.txt`。桌面端产物目前**未做代码
  签名**，请以校验和与源码比对为准。
