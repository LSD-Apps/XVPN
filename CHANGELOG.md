# 更新日志 · Changelog

本文件记录 XVPN 的每个公开版本。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

> 版本号的**唯一来源**是 [`app/pubspec.yaml`](app/pubspec.yaml) 的 `version:`
> （去掉 `+build` 后缀）。发布 tag 必须与它一致，否则发布流水线会直接失败——
> 见 [`.github/workflows/release.yml`](.github/workflows/release.yml)。

## [1.1.0] - 2026-09-13

本次发布的主题是**分流精度**：既补上此前完全看不见的「误入隧道」，也修掉两处
会让判定与实际行为不一致的结构性问题。

### 新增

- **内置国内域名补充规则集**（`geosite-cn-extra`，**默认启用**）。由
  `scripts/build-cn-domain-ruleset.ps1` 从 `felixonmars/dnsmasq-china-list`
  （WTFPL v2）编译，110454 条 `domain_suffix`。默认启用是实测结论：
  召回 40/40（100%），误命中约 150 个境外域名（含被墙服务类别）**0 个**；
  且万一有域名被错列，它被判直连后连接失败会被自动纠正表学成强制代理，
  而学到的规则排在规则库之前，因此这类错误可自愈。
- **隧道流量去向**：「分流规则」页新增清单，把已观测到的走隧道目标按流量降序排出，
  标注每条「为什么在隧道里」（命中规则库 / 有专门规则 / 默认规则兜底），并提供
  就地「改为直连」。它补的是一个此前完全不可见的缺口：**误入隧道的流量不失败、
  不报错**，只白占隧道带宽，用户看不出原因。
- **反方向自动纠正**：程序原先只会往隧道里推流量（依据「判为直连却失败」），
  现在也会把**本该直连却走了隧道**的域名拉出来——依据是 DNS 交叉校验给出的
  **事实**（该域名的直连解析落在 `geoip-cn` 网段内），连续 2 次独立测量后改为直连。
  与正向纠正共用同一张表、同一注入点与同一套撤销/衰减规则；任何一次直连失败都会
  清零计数并把规则改回走隧道。测量绕过 DNS 校验缓存，否则第二次「证据」只是重读
  同一次结果。
- **直连白名单**：把「确实能直连、却被默认规则送进隧道」的域名显式列出，两类：
  * **国内长尾站点补充**（默认启用，`gaoding.com` / `jianyu360.com` /
    `gelonghui.com` / `chuangkit.com`）——实测 55 个国内站点中 `geosite-cn`
    漏掉的那 4 个，其直连解析全部落在 `geoip-cn` 网段内；
  * **境外应用预置**（Cursor，按需启用）。
  两者都不依赖解析结果，因此不受 DNS 污染影响；改动在下一次连接时写进内核配置。
- **「推荐规则集」区块**：把覆盖更全但许可有灰区的第三方规则集（ChinaMax 系）
  以「只给地址、不再分发」的方式提供给用户一键添加。
- **DNS 决策跟随路由决策**。此前 `dns.rules` 只认 `geosite-cn`，而路由决策来自
  多个来源，两者会互相矛盾：已判定该直连的域名仍被境外解析器解析（于是拿到境外
  CDN 的地址再去直连），因境内答案不可信而改成走隧道的域名却仍被送去境内解析器。
  现在 DNS 与路由从**同一个**决策表派生，且**全部域名类规则集**一起参与
  （「是否域名类」由内置定义逐条声明，不再靠 `tag == 'geosite-cn'` 硬编码）。
- **「隧道 DNS」自检探针**。原先那条 DNS 探针用的是 `www.baidu.com`，命中
  `geosite-cn` 因而走直连解析器——从未碰过隧道解析器，而后者是所有境外站点的
  解析出口。新增探针用不命中规则集的域名，并新增「隧道不通 DNS」结论
  （多半是节点不允许 UDP/53 出站），与「DNS 解析异常」分开归因。

### 测量（可复核）

- 新增 `scripts/measure-ruleset-runtime-cost.ps1`：交替采样的规则集**运行时**开销
  对照（此前只量过 `sing-box check`，那是配置校验不是运行时）。实测 522 KB /
  11 万条规则集的代价是约 **1.2 MB 常驻内存**，启动与每请求延迟差异在噪声内。
- 新增 `scripts/review-cn-ruleset-quality.ps1`：固定随机种子的清单组成抽样。
  实测 64% 解析到国内、20.7% 为死域名、15.3% 未判为国内——后者不必然是误命中。
- 两个脚本都只用随包内核 + 本地死端口出站，不产生到任何真实服务器的流量。
- 附带发现：`geoip-cn` **整块缺失 `8.0.0.0/8`**（含阿里云国内段；8.x 前缀为 0 条），
  但主流国内站点解析出的 146 个 IP 全部命中，因此缺口真实但很窄。影响面精确限定为
  IP 字面量目标与 `classifyRegion`（进而影响反方向纠正的触发与 DNS 交叉校验的结论），
  域名路由不受影响。修复方案已记入 [`docs/RULES.md`](docs/RULES.md) 的未实现清单。

### 修正

- **推荐规则集不参与 DNS 分流**（自己上一轮留下的缺口）。「是否域名类」先是靠
  `tag == 'geosite-cn'` 硬编码，改成由内置定义声明后**自定义条目仍一律按非域名类**
  处理——而「推荐规则集」（`cn-large`）走的正是自定义路径。于是我们主动推荐给用户
  的规则集，被它判为直连的域名仍经隧道解析，等于从推荐入口把同一类不一致重新引入。
  现在标记随条目走并持久化进存档：内置在定义里声明，推荐项由
  `SuggestedRuleSet.domainRuleSet` 带上，手工新增的仍按非域名类（类型无从得知）。
  存档兼容性两处都做了处理并有测试锁定：`updatable` 缺失默认 `true`，
  `domainRuleSet` 缺失**回退到内置定义**（否则升级用户会静默失去「命中规则集的
  域名走直连解析器」）。
- **`.cn` 曾整批缺失**（实测发现的构建缺陷）。上游**故意不列举** `.cn` 等中文
  域名，靠 dnsmasq 的 `server=/cn/` 这类顶级规则表达（110460 个条目里含 `.cn`
  的为 0）。第一版构建脚本把单标签条目当「不是域名」跳过了，于是产物系统性缺失
  整批 `.cn`；症状是 40 个国内站点里漏掉的 6 个**全是 `.cn`**，而它们能命中
  `geosite-cn`，因此寻常检查看不出来。修法是保留单标签条目（与 dnsmasq 语义
  等价，已用内核验证标签边界），召回率从 85% 提升到 100%。脚本自检现在显式
  断言顶级规则存在、标签边界正确、境外域名不命中。
- **分流优先级写成显式契约**。`ip_is_private` 原先被放在整张决策表**之后**，
  于是程序学到或内置白名单的 `forceProxy` 能覆盖它。内网主机名会落进这两类证据
  范围（`nas.local` 临时不可达就会攒够「直连失败」），结果是内网流量被送进隧道，
  既费流量又必然连不上。现在按「用户 → 内网直连 → 学到/白名单 → 规则库 → final」
  分五段，由 `buildRouteRules()` 按段返回保证，并有测试直接断言下标关系。
- **出站标签改为单一来源**（`OutboundTags`）。`'vpn'` 此前在 5 处被重复写成字面量，
  风险不对称且隐蔽：改了标签会让路由照常工作、而所有按出站判定的统计**静默失效**
  （界面显示「走隧道 0 B」而隧道在工作）。
- **流量增量按 `(目标, 走向)` 归并**。此前只按目标归并，同一个目标在同一秒内既有
  直连又有代理连接时，两路字节会被合并记到「最后一条连接」的方向上。
- `RuleSetEntry` 新增 `updatable`：构建期产物不能用「检查更新」下载，此前会让
  用户拿到一份配置文本并被魔数拒绝，报错却指向他的网络。
- 「恢复内置规则」的说明补齐：明确列出不会被改动的三项（手工规则、自定义与推荐
  规则集、直连白名单开关）。
- **文档纠正了一处被实测否证的结论**：原先写「域名未命中 `geosite-cn` 时，内核会
  先解析域名再拿结果匹配 `geoip-cn`，因此长尾站点仍会走直连」。用随包内核做的受控
  实验否证了这一点——**判定过程中不会发生任何域名解析**，`geoip-cn` 只对**目标
  本身就是 IP** 的连接生效。方法与数据已写入文档，README 里对应的「双保险」
  表述也一并更正。

## [1.0.0] - 2026-09-12

首个公开版本。仓库自 2026-09-10 起开发，因此本节描述的是**当前真实落地的功能
集**，而不是相对某个更早版本的增量；所列内容均来自仓库历史与实现，未做前瞻性
承诺。

### 新增

**协议与配置**
- WireGuard（`.conf`）、OpenVPN（`.ovpn`）、Hysteria2（`.yaml` / `.yml`）：
  一律按**内容**识别，不靠扩展名——因此改名也能导入；每个协议有约定的后缀以便
  区分，但约定不是准入条件。分享链接与 sing-box 出站 JSON 同样可以解析。
- 参数规范化层：把加密套件名、摘要名、MTU 等归一化成内核认可的写法，认不出的
  直接剔除——这些参数写错会让内核**直接启动失败**，而错误信息只面向开发者。
- 导入路径：拖拽、系统文件选择器、粘贴文本；多份配置的记忆与切换。
- 配置与凭据的本地保护：Windows 用 DPAPI、Linux 优先用系统钥匙串
  （libsecret）；钥匙串不可用时降级为明文，并在界面**如实告知**。

**分流与 DNS**
- **「分流规则」页**：桌面侧栏（位于「设置」之上）与移动端底部标签各有一项，集中
  维护分流相关的一切，设置页不再承载这些内容。
  - **规则集**：内置 `geosite-cn` + `geoip-cn` 可启用/停用/删除/新增自定义，并可
    「恢复内置规则」；自定义规则集会被真正写进内核配置（禁用或删除的不会出现）。
  - **域名分流规则**：分「默认规则」（不可删除）、「程序学到」与「手工指定」三组，
    可新增/编辑/删除；自动纠正学到的规则可逐条撤销。
  - **分流模式**（智能分流 / 全局代理 / 全局直连）也移到这里。
  - 「恢复内置规则」的作用域是：重新启用两个内置规则集，并清除**程序学到**的规则，
    **保留手工指定**的规则与自定义规则集；执行前会明确说明。
- 规则集随包分发，首次连接解包到应用私有目录，可「检查更新」增量刷新。
- 双解析器 DNS：命中规则集的域名走直连解析器，其余在隧道内解析，避免不一致的
  答案驱动分流。
- DNS 交叉校验：同一域名分别用直连解析器与内核 `/dns/query` 解析，比较答案集
  并用 `geoip-cn` 前缀索引判断归属，区分「双部署」与「答案不一致」。
- 自动纠正：连续「判为直连却失败」的域名会被改走隧道，并以最高优先级注入路由
  规则；一次直连成功即清零失败计数，两次成功则撤销——反证优先于猜测。

**诊断与观测**
- 失败归因区分三类：直连失败（本地网络 / DNS）、隧道失败（节点 / 服务器）、
  规则覆盖缺口；启动自检分别验证两条腿。
- 连接列表、按出站的字节统计与实时速率来自内核 Clash API（仅回环 `127.0.0.1`）。
- 自动恢复：内核崩溃 / 隧道不通 / 进程卡住时带次数与冷却限制地重启；
  2080 / 2081 端口被占用时自动换端口。

**平台与界面**
- **Windows** 桌面端：无边框自绘标题栏、系统托盘、系统代理接管与还原
  （免管理员权限）。
- **Android** 端：`VpnService` 提供 TUN，`libbox` 作为进程内原生库。
- **Linux** 桌面端：与 Windows 相同的「子进程内核 + 系统代理」模型，无需 root；
  桌面环境支持 GNOME 系（gsettings）与 KDE；X11 下自绘标题栏、Wayland 下回退
  系统装饰。
- **系统托盘**（Windows 与 Linux）：显示版本号与连接状态，未连接时图标置灰，有可用
  更新时提示新版本；关闭主窗口收进托盘、连接不中断。Linux 走 StatusNotifierItem
  （libayatana-appindicator3，运行期 `dlopen`，装不到就降级为无托盘），且托盘退出与
  `SIGTERM` 都先由 Dart 还原系统代理、结束内核后再退出。
- 一套状态层、两种布局：以 900px 为界切换桌面侧栏与移动底部标签；两端呈现结构
  由 `app/test/platform_parity_test.dart` 的断言守住。

**应用内更新**
- 启动时**静默检查一次**更新：每次启动在后台查询一次 GitHub Releases，仅在发现
  新版本时提示。桌面端在标题栏出现一个可点击的提示入口，移动端在设置页「版本
  更新」卡片呈现——卡片直接显示启动检查发现的版本，无需用户先点「检查更新」。
  不会自动下载、不会弹窗打断；检查失败与「没有新版本」在体验上完全一致。
- 手动「检查更新」→ 下载（带进度、可取消）→ **校验 `SHA256SUMS.txt` 通过后**
  才安装；桌面端在用户确认后自替换并重启，Android 交由系统安装器。提示可「忽略」，
  忽略后本次运行不再打扰。

### 安全与合规

- 采用 **GPL-3.0-or-later**：受内置内核
  [sing-box](https://github.com/SagerNet/sing-box)（GPL-3.0-or-later，`v1.14.0`）
  约束——Android 端以原生库形式把它链接进同一进程，构成 GPL 意义上的组合作品。
- 第三方声明见 [`NOTICE.md`](NOTICE.md)：sing-box（含上游「不得暗示关联」附加
  条款与对应源码获取方式）、GPL-3.0-or-later 的规则库、Dart/Flutter 依赖、
  字体图标、出口管制提示；内核静态内嵌的 Go 依赖许可由
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) 聚合列出（sing-box `v1.14.0`
  下 111 个模块，含许可标识与全文）。
- 发布产物**随附许可证**：Windows / Linux 压缩包根含 `LICENSE`、`NOTICE.md` 与
  `THIRD-PARTY-NOTICES.md`，Android APK 内含 `assets/licenses/` 下的同名文件。
- 应用内**设置页「关于 → 开源许可」**可读上述三份文本与 Flutter 生成的
  `NOTICES.Z`，无需离开应用。
- 隐私政策 [`PRIVACY.md`](PRIVACY.md) 逐项列出本地数据与应用会发起的全部网络
  连接；应用不集成任何分析、广告或崩溃上报 SDK，也不运营后端。
- 新增 [`SECURITY.md`](SECURITY.md)（私密漏洞报告渠道）、本文件与
  [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。

### 已知限制

- 桌面端只接管**认系统代理**的程序：浏览器与多数桌面软件生效，游戏与命令行
  工具不生效。桌面端 TUN 需要 wintun 驱动或提权辅助进程，当前版本未内置，
  设置页**刻意不提供**该选项，而不是给一个拨了没反应的开关。
- 尚无 iOS 工程。

[1.1.0]: https://github.com/LSD-Apps/XVPN/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/LSD-Apps/XVPN/releases/tag/v1.0.0
