# 更新日志 · Changelog

本文件记录 XVPN 的每个公开版本。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

> 版本号的**唯一来源**是 [`app/pubspec.yaml`](app/pubspec.yaml) 的 `version:`
> （去掉 `+build` 后缀）。发布 tag 必须与它一致，否则发布流水线会直接失败——
> 见 [`.github/workflows/release.yml`](.github/workflows/release.yml)。

## [1.4.0] - 2026-09-17

### 新增

- **「随系统启动」真的能用了，并且托盘菜单里可以直接开关。** 托盘右键菜单多了
  一项「开机自动启动」（复选），设置页「启动」卡片里也多了一个同名开关，两处显示
  同一个状态。

  这一项**此前被移除过**，原因是它只把一个布尔值存进设置文件，从来没有任何代码把
  它落到系统的启动项里——一个拨了不会有任何反应的开关比没有这个开关更糟。现在后端
  落地了，因此加了回来，并且区分两种运行形态（见
  [`app/windows/runner/auto_start.cc`](app/windows/runner/auto_start.cc)）：

  - **绿色解压版**（zip）：写 `HKCU\...\CurrentVersion\Run`，与以往一致；
  - **MSIX 安装版**：走系统托管的 `Windows.ApplicationModel.StartupTask`。
    MSIX 包内**写不了** Run 键——写入被重定向到包私有的虚拟存储，而 Windows 不会
    为虚拟存储里的 Run 项创建登录启动项，于是「开关拨过去了、写注册表也返回成功，
    下次开机却什么也没发生」，没有任何报错。这是本次改动里最容易被做成静默失效的
    一处。

  状态的事实来源**在系统里**：用户随时能在「任务管理器 → 启动」或
  「设置 → 应用 → 启动」里改它，因此每次启动都会回读一次并校准界面，而不是信一份
  自己维护的存档副本。后端在当前形态下不可用时（MSIX 包没声明扩展、通道缺失），
  设置页**不渲染**这一行、托盘菜单项灰掉，而不是给一个假的开关。

  开启后开机时应用会自动启动并显示主界面；**不会**自动拨号（连接仍由用户或
  「导入后自动连接」那条规则决定）。

- **Windows MSIX 安装包**（`XVPN-<ver>-windows-x64.msix`）。打包脚本
  [`scripts/package-msix.ps1`](scripts/package-msix.ps1)，安装与桌面快捷方式由
  [`scripts/install-msix.ps1`](scripts/install-msix.ps1) 完成，两者都接进了
  [`scripts/build-release.ps1`](scripts/build-release.ps1)（`-Msix`）与发布流水线。

  安装后**自动创建桌面快捷方式**。MSIX 没有「安装完成后运行脚本」的标准钩子，而
  打包进程写 `%USERPROFILE%\Desktop` 会被文件系统虚拟化到包私有目录、写不到真实
  桌面，因此快捷方式由一个未打包的进程（安装脚本）创建。它的目标指向清单声明的
  执行别名 `%LOCALAPPDATA%\Microsoft\WindowsApps\xvpn.exe`，而不是
  `%ProgramFiles%\WindowsApps\XVPN_<版本>_x64__<哈希>\` 下那个带版本号的真实路径
  ——后者每次升级都会变，指向它的快捷方式升级后就是死链接。

  许可与第三方声明（`LICENSE` / `NOTICE.md` / `THIRD-PARTY-NOTICES.md`）同样随
  MSIX 分发，并在流水线里断言它们真的在包内。

  > **自签名包的安装需要管理员提权一次**。实测踩到并已修：证书放进
  > `CurrentUser\Root` + `CurrentUser\TrustedPeople` 后 `signtool verify /pa`
  > 报 0 错误，`Add-AppxPackage` 却仍以 `0x800B0109` 失败——**AppX 部署服务以
  > SYSTEM 身份运行，不读 CurrentUser 证书存储**，自签名证书必须落到机器级。
  > `install-msix.ps1` 现在会检测提权状态、写对应作用域的存储，未提权时明确
  > 说明下一步该做什么，而不是把 HRESULT 甩给用户。另一处实测修正：写
  > `CurrentUser\Root` 不能用 `Import-Certificate`（报 "UI is not allowed in
  > this operation"），必须走 .NET 的 `X509Store`。

- [`app/test/msix_packaging_test.dart`](app/test/msix_packaging_test.dart)：守住
  清单与原生代码之间的文本契约。最贵的一条是 `windows.startupTask` 的 `TaskId`
  必须与 `auto_start.cc` 里的 `kStartupTaskId` 逐字一致——不一致时
  `StartupTask.GetAsync` 抛异常、原生按「后端不可用」处理，于是设置页里那一行
  **悄悄消失**，用户看到的是「这个版本没有开机自启」，而不是任何错误。

### 变更

- 设置页「启动」卡片由桌面端与移动端各自的写法合并成一个 `_buildStartupCard`，
  两端展示同一组选项；「随系统启动」只在后端可用时出现。

- **重做「分流规则 → 域名分流规则」卡片输入行下方的动作区**（桌面与移动端同一处
  代码）。此前它是四行同款灰色文字链接平铺在卡片底部：导出规则包、导入规则包、
  清理过期、全部清除。问题不是「不好看」，而是四条可被证伪的缺陷：

  - **不相干的动作被平铺成同一个列表。** 导出/导入（把规则变成文件）与
    清理/全部清除（删本机数据）语义、风险、频率都不同，却都是等权的 12.5px 灰字。
    实测其在 1180px 下每个链接的命中盒是 **1110×40**（几乎整张卡片宽），在 390px
    下各自独占一行 324×40。这也违背项目自己的规范：
    `rules_screen.dart` 的卡片级动作（新增 / 检查更新 / 恢复内置规则）一律是
    真实按钮，`TapAction` 只用于列表项内部的逐条操作。
  - **不可逆的「全部清除」与「导出规则包」长得一模一样。**
  - **「清理过期（N 条中）」的计数口径是错的**：`N` 是全部条目（手工 + 学到 +
    白名单），而这个动作只删「程序学到」那一类。实测 1 条学习规则 + 4 条手工规则
    时标签写「8 条中」，其中可被清理的是 0 条。
  - **校验提示离输入框 108px，中间隔着两个可点动作**（导出、导入），
    错误提示因此脱离了它要解释的那个控件。

  现在分成两段、各带标题与一句说明：**规则包**（导出/导入成对、等宽并排、带图标）
  与**批量清理**（真实按钮；「全部清除」用 danger 变体并说明不会动手工规则；
  「清理过期」只数可清理的条数，为 0 时禁用）。校验提示移到输入行正下方并带图标，
  输入框同时支持回车提交。「最近一次自动纠正」从卡片最底部挪回「程序学到」一节——
  它解释的正是上面那些规则的来历，放在页脚离被解释对象隔了整张卡片。

  回归由 `app/test/auto_route_card_layout_test.dart` 守住（分组标题、控件类型、
  danger 变体、成对等宽、计数口径、提示与输入行的距离、回车提交）。
  改动前后的整卡渲染图见 `design/shots/v4-rules-card-{desktop,mobile}.png`。

### 修复

- **托盘菜单的「开机自动启动」勾的是缓存镜像，而这一项的事实只在系统里。**
  于是系统状态被应用之外改过之后（用户在「任务管理器 → 启动」或
  「设置 → 应用 → 启动」里动过它），托盘的勾会与真实状态**相反**——用户点它一下
  反而什么都没变，因为它以为要关，而系统本来就已经关着。体感就是「托盘菜单这一项
  没对接上」。

  现在弹托盘菜单之前**先回读一次系统**；回读结果与镜像不一致时，顺手把镜像和
  Dart 侧都校准过来，设置页的开关会跟着对齐。这条纪律写进了
  [`app/test/auto_start_abi_layout_test.dart`](app/test/auto_start_abi_layout_test.dart)
  （回读必须在 `AppendMenuW` 之前、且不一致时要通知 Dart）。

- **「随系统启动」在 MSIX 安装版里完全不起作用（拨了没反应），而它的根因是一处
  手写的接口 vtable 写错了。** 绿色解压版一直是好的（写注册表 Run 键）；只有打包
  形态那条 `Windows.ApplicationModel.StartupTask` 的路是坏的。

  开发时为了「顺手看一眼异步操作的进度」而复述了 `IAsyncOperation<T>` 的 vtable。
  但 SDK 里它是

  ```cpp
  template <class TResult>
  struct IAsyncOperation_impl : IInspectable   // 只继承 IInspectable
  ```

  并且**只有三个自有方法**：`put_Completed` / `get_Completed` / `GetResults`——
  **根本没有 `IAsyncInfo` 的 `get_Status`**（出处：
  `windows.foundation.collections.h`）。于是「按自己以为的顺序去调 `get_Status`」
  在第一次调用就访问冲突（`0xc0000005`）。改对顺序也没有用，因为顺序本身就不存在。

  这类错误的形态是**编译期零提示**：vtable 错位不会报错，只会在真机上以
  「访问冲突」或「读到垃圾指针」收场。中间还因此误判过好几次（以为是 STA 不能挂
  回调、以为是跨线程封送问题、以为是消息泵不对），绕了很远。

  现在的做法是**不再复述异步接口的 ABI**，只用 SDK 给的具体类型
  （`__FIAsyncOperation_1_...`）、只调确实存在的方法；完成信号一律由
  `put_Completed` 的回调给出，不再轮询 `get_Status`。

  为防止复发，新增 [`app/test/auto_start_abi_layout_test.dart`](app/test/auto_start_abi_layout_test.dart)：
  禁止再出现手写的异步 vtable、禁止出现 `get_Status`、要求用 MIDL 生成的类型别名、
  回调只继承一个接口且不在 `Invoke` 里自我释放、`GetResults` 必须在等到完成之后。

  实测（MSIX 安装版）：开关拨到开会把系统里的 StartupTask 状态从 `Disabled(0)`
  改成 `Enabled(2)`，关掉回到 `Disabled(0)`，重启后保持；托盘菜单项与设置页开关
  同步。绿色解压版仍走 Run 键，回归未受影响。

- `scripts/package-msix.ps1` 的**无时间戳回退路径根本执行不到**。代码里本来就有
  「带时间戳签名失败就改写不带时间戳的签名」，但 `signtool` 把错误写到 **stderr**，
  而脚本开头设了 `$ErrorActionPreference = 'Stop'`——于是这条**本可以被回退处理**
  的失败先抛了出去，整个构建白失败。实测：DigiCert 时间戳服务偶发不可达时，
  包其实完全可用。现在与 `deploy-android.ps1` 一致，临时放宽偏好、只看退出码。

- `scripts/deploy-android.ps1` 在**构建成功后仍以退出码 1 中止**。原因与脚本自己
  在 `Invoke-Adb` 里已经处理过的那条完全相同，但构建那一步漏了：`flutter build`
  会把**正常的**提示写到 stderr（「Flutter assets will be downloaded from ...」、
  插件 KGP 的弃用警告），而脚本开头设了 `$ErrorActionPreference = 'Stop'`，于是
  「stderr 有输出」被当成终止性错误——它在自己那句 `if ($LASTEXITCODE -ne 0)`
  **之前**就抛了出去，把一条成功消息报成失败，而 `build/app/outputs/flutter-apk/`
  下的 APK 其实是好的。现在与 `Invoke-Adb` 一致：临时放宽偏好，成败只看退出码。

  实测踩到：真机部署时脚本以退出码 1 结束，但 APK 已生成且可用。

- **MSIX 安装后桌面快捷方式是一块白板图标。** `install-msix.ps1` 把快捷方式的
  `IconLocation` 指到了它的**目标**——而那个目标是执行别名
  `%LOCALAPPDATA%\Microsoft\WindowsApps\xvpn.exe`，一个 **0 字节的重解析点**
  （属性 `ReparsePoint`），里面没有任何图标资源可提取。实测确认：包内真实的
  `xvpn.exe` 有图标，别名没有。

  也不能把图标指向包内那个带版本号的 exe
  （`...\WindowsApps\LUSIDA.XVPN_<版本>_x64__<哈希>\xvpn.exe`）——路径每次升级
  都会变，图标会静默退回白板。

  现在由 `Install-AppIcon` 取一份 .ico 放到稳定位置
  `%LOCALAPPDATA%\XVPN\app.ico`，快捷方式引用它（**目标仍是执行别名**——那个是
  稳定的，只有图标需要真实文件）。图标来源按优先级：显式给的 `.ico`（发布包里
  与 `.msix` 并排）→ 包内 `Assets\` 的 PNG 磁贴现场合成多帧 ico → 从 exe 抽单帧
  兜底。为此 `package-msix.ps1` 额外产出 `Assets\Icon-256.png`（不被清单引用，
  只作图标来源；小尺寸从它缩小比从 44 放大清楚得多）。

  另外修掉卸载时一处枚举失效：删证书的管道里直接在枚举过程中 `Remove-Item`，
  集合被改动后枚举器失效，于是打出一句**指纹为空**的「无法移除」——而那张证书
  其实已经删掉了。改为先物化成数组再遍历。

## [1.3.0] - 2026-09-15

本版把应用改名为「幽门」、安卓包名改成 `net.lusida.xvpnclient`，补齐 VMess /
VLESS / Trojan / Shadowsocks 与自备订阅，修掉安卓「连上了却打不开网页」的根因，
并在真机上把协议矩阵验到 7/7。

### 变更

- **应用中文名改为「幽门」，安卓包名改为 `net.lusida.xvpnclient`，作者与官网写明为
  LUSIDA / `www.lusida.net`。** 用户能看到的名字统一成「幽门」：安卓启动器标签、
  Windows 的窗口标题与托盘、Linux 的窗口标题与托盘、任务切换器、安卓 VPN 的会话名
  （系统「已连接」通知里显示的那一行）、Windows 可执行文件的属性页。仓库名、可执行
  文件名与发布附件仍叫 `XVPN`——附件改名会让已经装好的旧版本找不到升级包，代价远大于
  名字上的一致。设置页「关于」多了一行「作者与官网」，点开是产品主页；官网与仓库并列
  而不是互相取代：想下载、看说明去官网，想看代码、读许可全文去仓库。

  包名同步动了 `namespace` 与 Kotlin 源码目录（`net/lusida/xvpn` →
  `net/lusida/xvpnclient`），避免出现「代码在一个包、对外是另一个包」的长期错位；
  debug 版因此变成 `net.lusida.xvpnclient.dev`。**用旧包名装过的设备无法覆盖升级**，
  需要卸载重装，而卸载会连配置与凭据一起清掉。

- **安卓上的 `auth-user-pass` 账号密码不再明文落盘，改用系统 Keystore 加密。**
  Keystore 里放一把不可导出的 AES-256 包装密钥，启动时用它解开一把随机数据密钥
  （`credentials.key`），账号密码由后者以 AES-256-GCM 同步加解密——绕这一层是因为
  Keystore 只能经 Java 侧异步调用，而账号密码必须在界面第一帧之前就恢复出来，否则
  用户会先看到一份「没有密码的配置」，最坏情况下应用会带着空凭据去建隧道。与 Windows
  的 DPAPI 一样，这份密钥**绑定当前设备**：从云备份恢复、或换一台设备打开时解不开，
  此时应用把配置标记成「待补填账号密码」并提供填写入口，而不是让它消失。Keystore
  不可用或拿不到可写目录时如实退回明文，并在凭据表单里写明「未加密」。

- **「开源许可」不再把许可文本打进应用，改为跳转到项目主页的许可章节。** 应用内
  自绘的许可浏览界面，连同一并打包供它读取的 `LICENSE`、`NOTICE.md` 与
  `THIRD-PARTY-NOTICES.md` 一起删掉：那三份文本合计约 430 KB，其中绝大多数是
  111 个模块的聚合许可（`THIRD-PARTY-NOTICES.md` 单份就有 353 KB / 1683 段），
  没有人会在手机上逐条读，却要每一个安装包都为它付出体积。设置页「关于 →
  开源许可」现在用系统浏览器打开项目主页的许可章节，那里按需指向三份全文。

  合规要的是「接收者能拿到」，不是「长在安装包里」：正文继续随仓库与各平台发布
  包分发（Windows / Linux 压缩包根目录、APK 的 `assets/licenses/`），Release
  说明仍给出对应 tag 的源码地址。应用内仍随包的只剩 `app/assets/legal/LEGAL.md`
  ——法律与使用声明必须在离线时也读得到，那是**使用边界**，不是许可清单。

- **「版本更新」卡片改成与其它设置项一致的左右布局。** 此前是竖排：说明占一行、
  按钮另起一行。现在每个阶段都是一条设置行——左边是「当前版本 v…」加状态，右边
  是本阶段能做的动作，与「外观」「启动」「关于」用的是同一个 `SettingRow`，行距
  与控件右边缘因此对齐。进度条与「自行安装」的路径框仍是整行内容，它们表达的是
  整件事的进度，压在右栏里会和按钮抢宽度。失败与完成等阶段的状态改用行首的小
  标记（带图标，颜色之外还有形状），标题保持稳定，不再随状态在标题里换词。

### 修正

- **OpenVPN：`remote` 写成 IP 的配置一律连不上——1.3.0 里这个协议对这类配置是坏的。**
  服务商直接给 IP 端点是常态，所以这不是边角情况。

  现象是「界面显示已连接、什么都没通」：内核起来了、`openvpn-client` 端点也注册了，
  探针却一直失败，`/connections` 里没有一条走 `vpn`，而客户端侧看不到原因。

  根因是适配器无条件把 `tls.server_name` 写成了 `remoteHost`。sing-box 会拿它做
  **verify-x509-name 式的名字校验**，而对端证书签给的是域名——拿 IP 去比对必然失败。
  在 PC 上用同一份端点配置开 debug 日志跑，原话是：

  ```
  ERROR endpoint/openvpn-client[vpn]: client terminated:
        (peer certificate verification failed | peer certificate fails verify-x509-name check)
  ```

  去掉 `server_name` 之后同一份配置立刻变成 `tunnel established to <ip>:1194 over udp`。

  **OpenVPN 自身在没有 `verify-x509-name` 时并不校验主机名**，只校验证书链与
  `remote-cert-tls server` 要求的服务端用途，所以这里也照做：声明了 `verify-x509-name`
  就把名字写进 `server_name`（这也让上一版那条「名字断言没被执行」的提示变成**真实
  校验**），没声明就完全不写。SNI 不受影响——OpenVPN 默认也不发 SNI。

  这个缺陷 `sing-box check` 与单元测试都发现不了，因为它们都不做真实的 TLS 握手；
  它是拿**真机 + 真实节点**验出来的。协议矩阵随之补到 **7/7**：判据是内核探针经该出口
  返回 `delay`，且 `/connections` 出现 `chains` 含 `vpn` 的连接并带着增长的字节数——
  **不是界面上的绿环**。

- **OpenVPN 的默认端口被改错了：TCP 配置会去连 443，而规范里只有 1194。**
  这条是拿 OpenVPN 手册逐条核对时发现的，不是猜的——手册里 `--port` 是
  「TCP/UDP port number for both local and remote」，并明写「The current default
  of 1194 represents the official IANA port number assignment for OpenVPN」；
  `--remote host [port]` 省略端口时用的就是它，**与协议无关**。

  原实现把「TCP 常部署在 443」当成默认值，**解析器与表单生成器两处都这么写**。
  代价是静默连错端口：`proto tcp` + `remote host`（省略端口）会去连 `host:443`，
  而配置作者省略端口恰恰是因为服务端就在默认端口上；失败时界面只说「没能连上
  服务器」，没有任何线索指向端口被改过。两处必须同时改——只改解析器的话，
  确认导入会用表单**重新生成** `.ovpn`，把 443 又写回去。

- **`remote` 行自带的 proto 现在优先于全局 `proto`，且不再取决于两行的先后顺序。**
  此前两者共用一个变量，于是 `remote h 1194 udp` 后面跟 `proto tcp` 会得到 tcp，
  把两行对调又得到 udp——同一份配置换个行序就换了协议。手册对
  `--remote host [port] [proto]` 的说法是「proto indicates the protocol to use
  when connecting **with the remote**」，`--proto` 只是缺省值：更具体的赢。

- **`verify-x509-name` 的名字断言没有被执行，这一点现在会说出来。**
  这条指令要求「对端证书的 X.509 名字等于 name」，比 `remote-cert-tls server`
  的「只要是服务端证书」更强；而内核端点没有表达名字的字段。此前它被**默默**
  降级成后者的等价物——用户以为自己钉死了服务端身份，实际只校验了证书用途。

  现在：名字被解析出来、在配置详情里可见、确认导入后**原样写回**（生成器此前会
  把它整条丢掉，连提示一起——提示是按解析结果给的），并给一条 info 说明名字没有
  被比对。不写成 warn 是因为连接本身不受影响；必须写出来是因为受影响的是用户
  以为已经生效的那个保证。

  仍有**一处未验证**并已写进 `docs/PROTOCOLS.md`：`tls.server_name` 目前填的是
  `remoteHost`，它是否同时参与证书主机名校验尚未确认。OpenVPN 自身在没有
  `verify-x509-name` 时**不校验主机名**，若 sing-box 校验，则「remote 是 IP、
  证书 CN 是域名」这类常见配置会比 OpenVPN 更严而连不上。**只有拿真实服务端跑
  一次才能定论，不凭推断改。**

- **静态密钥模式（`secret` / `<secret>`）不再沉默地失败。** 它是与 TLS 并列的
  另一种 OpenVPN 模式，而 sing-box 的 `openvpn-client` 只实现 TLS 模式，这类配置
  握手必然失败。现在两种写法都会被识别（内联块会被内联正则整段剥掉，只看指令
  列表会漏掉 `<secret>`），并明确告知「无法建立连接」——照 AmneziaWG 的先例，
  识别出来并说清楚，而不是让用户对着一个连不上的隧道猜。

- **`docs/PROTOCOLS.md` 的协议状态表按实际验证强度重写。** 原先六个协议都写
  「已通过官方 `sing-box check`」，把**配置校验**与**能连上**混为一谈。现在区分：
  五个协议已真机连通（判据是接入端收到隧道侧流量，见 `docs/ANDROID.md` 8.5），
  **OpenVPN 单独标注为「仅配置校验通过，未做过端到端」**——sing-box 不提供
  OpenVPN 服务端，本机也没有可用的（装它要管理员权限）。已验证的只有两条：
  `.ovpn` 能生成结构完整的端点，且随包分发给安卓的 libbox 确实带 `with_openvpn`。

- **应用连接过一次之后，「把配置分享到幽门」就彻底失效了——没有弹窗、没有报错、
  什么都没有。** 冷启动时却完全正常，因此很容易被当成「偶发」。

  根因在 Flutter 的通道语义：`MethodChannel` 每条通道**只保留一个** handler，
  `setMethodCallHandler` 是**替换**而不是追加，被替换掉的那条推送不会报错、也不会
  落日志。而 `com.xvpn.xvpn/vpn` 这条通道上有两类互不相干的推送——原生分享进来的
  配置（`main.dart` 关心）与内核日志（`AndroidVpnCore` 关心）。两边各自注册，
  于是谁后注册谁生效：应用一旦连接过，`AndroidVpnCore` 就装上了自己的 handler，
  而它只认 `coreLog`，`sharedConfigAvailable` 从此无人处理。

  现在 handler 只在 `core/lib/android_channel.dart` 里装一次，所有兴趣方都收到
  每一条推送、各自忽略不关心的那些。`window_controls.dart` 早就为同一类问题留过
  一句「两个 setMethodCallHandler 的话，先注册的那条推送会被静默丢弃」——只是这条
  通道上没人照着做。

  **真机复核**：同一个 `vless://`/`trojan://` 链接，修复前冷启动弹确认框、应用已在
  运行时什么都不发生；修复后热态立即弹出确认框。
  `test/android_channel_test.dart` 守住它——那条断言测的不是「某函数被调用」，而是
  「两个兴趣方都能收到同一条推送」，正是旧实现做不到的事。

- **连接页那句「规则库 2 项」是个写死的常量，真机上一直是错的。** 文案是
  `命中规则集的流量直连，其余走隧道（规则库 2 项）`，其中 `2` 硬编码。写下它的
  那年，默认启用的内置规则集确实只有两份（`geosite-cn` / `geoip-cn`）；后来
  `geosite-cn-extra` 与 `geoip-cn-extra` 加了进来并同样默认启用，这句没跟着改——
  内核实际吃进去的是四份，界面报的是两份。更糟的是它**永远是 2**：用户在
  「分流规则」页停用或新增规则集时，这个数字一动不动。

  这不是无害的瑕疵。界面给出的数字与程序此刻真正在用的东西不符时，用户拿它去
  排查分流问题只会被带偏——而收口「配置里写了、程序却没用」这一类静默失效，
  正是这一版的主轴。现在项数取自 `state.ruleSets` 里**实际启用**的那些。

  同时补了一条 widget 用例守住它：断言项数等于启用数量，且停用一份后从 4 变成 3
  ——写死的常量过不了这条。真机复核：卡片显示「规则库 4 项」，与
  `configuration.json` 里交给内核的四份规则集一致。

  同一张卡片上，系统代理地址早就是取自内核实际值的（`takeOverEndpoint`，注释里
  明写「写死 2080 等于给用户一个抄不走、也用不上的地址」）。这一处只是漏了。

- **协议展示名不再散着写——「新增协议时界面不用改」这条承诺现在才真的成立。**
  `vpn_protocol.dart` 开头写着「新增协议只需三步，界面、导入流程、配置生成都
  不需要改动」，但界面里其实有两处又把协议名打了一遍：

  * 连接页那句「支持 WireGuard、OpenVPN、Shadowsocks、VMess / VLESS / Trojan 与
    Hysteria2」是手写的。这一版加进 VMess / VLESS / Trojan / Shadowsocks 时就得
    手工同步它，漏掉任何一处，界面就会**少说一个已经支持的协议**——用户据此以为
    它不支持，而那正是他手里配置的协议；
  * `ConfigFormModel.defaultName` 为四个协议各留了一条 switch 分支，外面还套着
    一条 `_ => '${protocol.label} 配置'` 兜底。两者结果完全一样，**只有
    `hysteria2` 那条不同**——而它不同，仅仅是因为 `label` 写成了 `Hysteria 2`
    （多一个空格），需要一条分支专门把空格盖回去。为修一处的笔误再加一处，
    名字就有了第二个真相。

  现在清单从 `importableProtocols` 派生（`supportedProtocolsText`），默认名直接由
  `label` 拼出，`label` 统一成 `Hysteria2`（与仓库文档、表单标题、界面文案一致）。
  新增协议因此真的只改枚举与适配器两处。

  补了 `test/protocol_naming_test.dart` 守住它：展示名非空且互不相同、界面文案必须
  覆盖每个可导入协议、默认名必须等于 `'${protocol.label} 配置'`。最后一条正是用来
  挡住「再为某个协议单独留一条分支」。

- **连接失败后，圆环不再退回「未连接」，而是标出失败并写清下一步。** 此前一次失败的
  尝试只会让圆环变回中性灰、写着「未连接」，与「从来没点过连接」长得一模一样；那条
  报错 SnackBar 四秒后就消失，用户隔一会儿回到屏幕前只看到一个不说话的圆环——失败
  既没留下痕迹，也没有可以再试一次的落点。现在失败是一种独立的圆环状态：红色渐变环
  加一层更重的红光晕（比绿色连接态更显眼，它对应用户必须看到的结论），标题「连接
  失败」、副标「点击重试」，点圆环即重新连接；旁边一行给出失败结论，并带「知道了」
  把红色撤掉，不想重连的人不必被迫连一次。失败结论与一次性的 `lastError` 分开保存：
  前者只在下一次尝试开始、连上、或用户主动断开时清除，后者照旧由 SnackBar 消费。
  只有**连接过程中**报的错会进入这个状态，导入失败、规则库更新失败、以及已连上之后
  的健康自愈告警都不会把圆环染红——它们与「刚才那次连接」不是一回事。

- **失败行上只留一句人话，内核原文收进「详情」。** 此前那一行原样搬的是内核报错，
  实测出现过 `内核启动失败：parse rule-set: open /data/user/0/net.lusida.xvpnclient/
  files/rulesets/geosite-cn-extra.srs: no such file or directory` 这种整段——用户看不
  懂，也没有能做的事，唯一的效果是让人以为自己弄坏了什么。现在主视线上是
  `没能连上服务器` 一句，右侧一个「详情」把原文一字不删地给出来（可选中、可复制，
  反馈问题时照样附得上），再右边才是「知道了」。连接前的指令类失败（「需要账号密码，
  请先在配置页填写后再连接」）本来就没有原文，因此不摆「详情」入口。同一轮失败也不再
  弹一条原文 SnackBar：圆环与这行已经把话说完了，那条四秒的提示只会重复同一件事。
  刻意不做「智能分类」：内核报错有几十种，凭字符串猜出来的结论一旦猜错就会把用户引
  到错误的方向，而「没能连上服务器」对所有这些情况都成立。

- **安卓上默认启用的两份补充规则集根本没有被解包出来，第一次连接必定失败。**
  「安装包里该有哪些规则集」这件事有两份答案：`RuleSetStore.builtins` 一份，安卓的
  资源清单里又抄了一份。`geosite-cn-extra` / `geoip-cn-extra` 加进前者并默认启用之后，
  后者没跟着改，于是内核启动的第一件事就是
  `parse rule-set: open .../files/rulesets/geosite-cn-extra.srs: no such file or
  directory`，而界面只说「连不上」——原因与用户的网络、配置毫无关系。现在两份清单
  合一：安卓解包与桌面端的存在性自检都从 `RuleSetStore.builtins` 派生，新增内置规则集
  不会再有「只改了一处」的可能。这条是真机验证时复现出来的。

- **更新失败时安装包不再被一并删掉**。助手原先在收尾处无条件清理暂存目录，于是
  「自动替换失败」之后，那个已下载并校验过的安装包也消失了——而更新界面上刚刚
  才显示过「可以自行安装：解压覆盖安装目录」。现在只有**成功**才清理；失败时保留
  安装包与日志，并在日志里写明安装包路径。

  同时补上 Windows 失败分支的日志：提权那条路上用户点「否」时，日志与那份包是
  他仅有的两条线索。

- **Linux 助手的复制失败不再被当成成功**。`cp` 的退出码此前没有检查：只读挂载、
  文件被占用或磁盘满时，脚本会照常往下走、打印「重新启动」并清理现场，用户看到
  的是「已经更新」的假象，而安装目录可能只被替换了一半。**把失败当成功是这类
  脚本最坏的形态**——它比直接报错更难发现。

  现在每一步（`unzip`、`cp`）都检查退出码，失败即中止、保留安装包、重新拉起旧
  版本并以非零码退出。中止路径抽成一个 `fail` 函数，避免四五个调用点里漏掉
  「保留安装包」这一步。

- **「规则集大小」不再永远显示「尚未量过」**。内置规则集的 `sizeBytes` 起初一律
  是 0（`BuiltinRuleSet.toEntry` 是无 IO 的纯转换），而只有「检查更新」成功才会
  量一次——于是「分流规则」页里那条「大小」在绝大多数机器上永远是「尚未量过」，
  尽管程序刚刚亲手把这份文件解包到了磁盘。现在启动时与首次连接解包之后各量一次，
  按磁盘上的真实文件补上（目录问内核要，与「检查更新」同一个来源，因此安卓的
  APK 解包目录不会走错）。磁盘上确实没有副本时保持「尚未量过」——那才是诚实的。

- `startup_self_check.dart` 的注释引用了「`docs/RULES.md` 列出『尚未实现』的第二
  项」，而该清单已重排、这一项也早已实现。改为陈述事实（为什么隧道解析要单独成
  一条探针），不再引用一个会移动的位置。

### 新增

- **`docs/PROTOCOLS.md` 新增 3.5.3d**，完整记下上面这一处：现场、根因、内核原话、修法，
  以及为什么只有真机加真实节点才能发现它。
- **`docs/ANDROID.md` 8.5 补上「用真实节点怎么验」**：自建接入端那套判据在真实节点上
  用不了（没有服务端日志），换成客户端侧两条可核验的证据；并记下排查手法——取应用
  自己生成的 `files/configuration.json` 里的端点，在 PC 上配最小 sing-box 开 debug 日志跑。

- **下载完成后显示安装包位置，并给出「自行安装」的两个动作**。此前卡片的确认
  步骤只说「更新包已下载并通过校验」，安装包落在哪全靠用户自己猜（它在下一次
  冷启动可能就被清掉的临时目录里）。自动替换**不是唯一出路**——权限、杀毒软件、
  只读目录都会让它失败——而那时用户手里已经有一个校验通过的安装包，只要知道
  它在哪，解压覆盖安装目录就行，或拷到另一台机器上使用。

  现在确认步骤会显示**可选中的明文路径**（带文件大小），并提供：

  * **复制路径** —— 粘贴进文件管理器、或发给另一台机器；
  * **打开所在文件夹** —— Windows 用 `explorer /select,` 直接**选中**该文件
    （只打开父目录会让人再找一遍），Linux 退而打开所在目录；打不开时如实提示
    「可复制路径后自行打开」，而不是什么都不发生。

  同一条路径在**安装失败**时也会显示——那正是最需要它的时刻。

  安卓不显示：APK 落在应用私有缓存（`updates/`，靠 FileProvider 交给系统安装器），
  那个路径对用户既没有意义也打不开。

- **Shadowsocks 导入。** `ss://` 分享链接（SIP002 明文 / base64 userinfo / 旧式整串
  base64）与 sing-box 出站 JSON；插件拆成 `plugin` + `plugin_opts`。不认识的加密
  方法在解析阶段拒绝。多节点 Clash `proxies:` 走自备订阅入口（不引入 yaml 包）。

- **法律与使用声明补齐。** 中英 `docs/LEGAL.md` / `LEGAL.en.md` 写明预期用途、
  本仓库不受理的内容（节点/订阅/规避管制指南）、与 GitHub 可接受使用政策的对齐，
  并明确「开源不等于在每一地都自动合法」。设置页可离线阅读全文；GitHub 用 issue
  模板挡住「要节点」类请求。全新安装首次启动须确认已了解后再进入界面；升级前
  已有存档的安装不挡。

- **VMess / VLESS / Trojan 导入。** 分享链接与 sing-box 出站 JSON；传输层与 TLS
  （含 Reality）共用一份解析。VMess 的 `aid` 同时接受数字与字符串。Reality 缺
  公钥在解析阶段拒绝。多节点 Clash 走自备订阅入口。

- **国内 IP 前缀索引纳入 IPv6，并与规则集同源校验。** `geoip-cn.srs` 里本来就有
  两千多条 IPv6，旧生成器整表丢掉，纯 IPv6 的 DNS 答案因此永远「无法判断」。
  现在 `cn-ip.bin` 是 CIP2（IPv4 + IPv6）；补充规则集并入 `china6.txt` 与
  分运营商表（电信 / 移动 / 联通 / 教育网 / 科技网 / 鹏博士 / Google CN）。
  `china.txt` 不是这些分表的并集。启动时对照 `cn-ip.origin.json` 里的 `.srs`
  指纹，脱节会在分流规则页可见。安卓升级会覆盖解包目录里的索引，避免旧 CIP1
  粘住。

- **自备订阅与多节点导入。** 用户粘贴已有的 http(s) 地址，或导入分享链接列表 /
  Clash `proxies:` / sing-box `outbounds[]`。不内置任何 URL。节点服务器是 IP
  时用 `cn-ip.bin` 判定地区；落在国内网段时连接页说明境外流量仍进这条隧道，
  不自动改分流模式。配置身份是「协议 + 正文」，而**归属是一份集合**：同一个
  节点出现在两份来源里仍只保留一条，但两边都算它——记成单一归属会让刷新把
  归属抢走、让删除看不见另一份来源还在用它。

- **域名分流规则可导出 / 导入。** `xvpn-route-pack` JSON，导入后变成手工规则。
  没有中央共享服务。

- **反方向纠正用真实内核验证热生效。** 学成直连后，运行中的内核把该目标从拦下
  改成放行，不需要重连。

- **自动更新可以在受保护目录里完成（Windows）**。此前若把 XVPN 解压到
  `C:\Program Files` 这类需要管理员权限的位置，更新会被诚实地拒绝——用户只能
  手动覆盖。现在界面会先说明「写入需要管理员权限」，用户点「以管理员身份更新」
  后由系统弹一次 UAC 完成替换。

  设计上只把**一条 `Copy-Item`** 交给管理员进程，助手自身始终以普通权限运行。
  这一点是整套流程里最要紧的决定，因此单独记下：若反过来让助手以管理员运行，
  它重启出来的 XVPN 也会是管理员身份；而 UAC 若是由**另一个**管理员账户确认的
  （标准用户 + 管理员凭据是常见配置），那个进程读的是该账户的 `%LOCALAPPDATA%`，
  用户看到的会是「配置与凭据全部不见了」。把权限收窄到一条复制命令，这一整类
  问题都不会发生，也顺带不必去用「从提权进程降权重启」那种依赖 explorer 的
  取巧办法。

  用户点「否」时**一条文件都不会被复制**（助手在拿到授权前不开始复制），日志
  记录原因并重新拉起旧版本；日志现在会保留下来，因为提权这条路上它是事后唯一
  能查到原因的入口。

- **把「别装在受保护目录」变成可见的建议**。绿色分发的应用只要解压到用户目录
  （`%LOCALAPPDATA%\Programs\XVPN`、`~/.local/opt/xvpn`），更新就永远不需要授权。
  这条出路现在写在 README、使用指南里，也在更新界面拒绝/要求提权时**就地**给出
  （`suggestedUserInstallDir`），而不是只留一句「请手动更新」。

### 修正

- **安装路径含单引号时，更新助手脚本会整份失效**。日志行把安装路径直接嵌进了
  PowerShell 单引号字符串（`Write-Log '覆盖安装目录 C:\…\Bob's VPN'`），路径里的
  `'` 会提前结束字符串，从那一行起整份脚本语法就是坏的——报出来的是「Try 语句
  缺少 Catch / Finally」，真正的原因却在几十行之前。此前一直没被发现，是因为它
  只在安装路径恰好含单引号时触发。

  修法是先用 `_psQuote` 转义再拼。同时补了一条用**真正的解释器**做语法检查的
  用例：Windows 交给 PowerShell 的 `Parser::ParseFile`，Linux 用 `sh -n`。字符串
  断言只能证明「该有的片段在」，证明不了整份脚本能被解析——而一段语法错误的更新
  脚本，只会在用户机器上、更新进行到一半时才暴露，那时应用已经退出、安装目录
  可能正被改写。

- **`docs/RESILIENCE.md` 仍写「安卓还不自愈重启」**，而代码里 `_restartTunnel`
  早已接线。改为「已接线、缺真机确认授权框」，避免文档把已实现的能力说成缺口。

- **Linux 上的只读安装给出真实出路**。原先只写「请用系统包管理器更新，或手动
  下载解压覆盖」，现在补上用户目录这条路径，并说明为什么不能自行提权：
  `/usr/bin`、`/usr/lib` 归包管理器所有，绕过 dpkg/rpm 覆盖文件会破坏包数据库，
  下一次包升级又会把它们改回去。因此 Linux 上**即便用户要求提权也不做**。

- **主窗口默认居中**。此前窗口位置写死在 `(10, 10)`：2K / 4K 屏与 150%、200%
  缩放下它会明显偏在左上角，多显示器时还可能开在用户没在看的那块屏上，每次
  启动都要先把它拖到中间。

  现在默认居中到**光标所在显示器的工作区**（`rcWork`，已排除任务栏，因此窗口
  不会被任务栏挡住）。两处边界情况单独处理：

  * 窗口比工作区还大时（小屏 + 高缩放）居中的结果是负数，会让标题栏连同拖动区
    与关闭按钮一起落到屏幕外——此时退化为贴住工作区左上角；
  * 问不到显示器信息时退回原来的固定落点，而不是猜一个位置。

  Windows 侧由 `Win32Window::CenteredOrigin` 计算并返回**逻辑**像素，供
  `Create` 按同一块显示器的 DPI 缩放（两级换算必须同比例，否则高 DPI 屏上会
  偏离中心）。为此把 `Win32Window::Point` 的成员由 `unsigned int` 改为 `int`：
  Windows 坐标是虚拟屏幕坐标，主显示器左侧/上方的显示器落在负区间，用无符号
  会让「居中到左侧那块屏」绕回屏幕右端。

  Linux 侧在窗口**显示之后**用 `gtk_window_move` 居中。两个约束写在这里：
  不能用 `gtk_window_set_position(GTK_WIN_POS_CENTER)`，它自 GTK 3.10 起就是
  deprecated，而 Linux 构建带 `-Wall -Werror`，会直接编译失败；也不能在
  `gtk_widget_show` 之前调用 `gtk_window_move`，GTK 文档明确说多数窗口管理器
  会忽略对**初始位置**的请求、只接受显示**之后**的移动请求。Wayland 下合成器
  按协议自行决定位置，此时这句是无害空操作。

- **「安卓自愈重启会不会再弹系统授权框」有了真机答案：不会。** 这一条此前挂在
  `docs/RESILIENCE.md` 的「已知限制」里，注明「**这一步只能真机验证**」——它确实
  只能真机验证，而在这一轮之前没人验过。

  现在补上实测（vivo V1838A / Android 10）：断开接入端触发自愈后，`diag.log` 里
  出现第二次 `openTun ok`，**fd 从 109 换成 104**——TUN 是真的被拆掉重建，而不是
  「只把内核重载了一下」；而 200 秒观察窗内
  `com.android.vpndialogs/ConfirmDialog` 一次都没有出现。原因在 Android 侧：
  VPN 同意记在**包**上而不是记在单次请求上，`VpnService.prepare()` 只在从未授权时
  返回非空 Intent，`MainActivity.prepareVpn` 拿到 null 就立刻回 true。撤销授权或
  卸载重装之后第一次仍会问，那是对的。

  仍未复验的是**真实公网节点**下的自愈：上面那次用的是本机自建的接入端
  （经 `adb reverse` 回连开发机），不是公网节点。

### 新增

- **真机部署收成一条命令：`scripts/deploy-android.ps1`。** 此前真机安装是一串
  只存在于脑子里的手工步骤，而其中的关键两步是错的，代价是每次安装都卡满超时。

  脚本负责构建、调设备、安装、启动与取证（`-Evidence` 会把解包出的规则集、
  `credentials.key` 的长度、`diag.log` 里的默认网卡与 `openTun` fd 一起打出来）。
  构建固定带 `--target-platform android-arm64`，理由与 `release.yml` 相同：
  `libbox.aar` 只有这一个 ABI，不加这个开关，debug 包会把三个 ABI 全塞进去
  （147 MB → 220 MB 以上），而那多出来的两个在运行时根本加载不到内核。
  它碰的设备设置只有「安装校验」这一类；关动画要 `-RelaxAnimations` 显式打开才做
  ——那改的是用户日常就能感觉到的系统行为，而脚本的职责只是把包装上。
  两处「厂商行为」被如实写进注释与 `docs/ANDROID.md`，因为它们都不是猜的：

  * **vivo / iQOO 每次安装非自家商店的应用都会弹 `PackageInterceptActivity`，
    它让 `adb install` 永远不返回。** `-g`、`-t`、以及伪装安装器
    （`pm install -i com.bbk.appstore`）都绕不过；只有
    「设置 → 应用安装 → 应用安全验证 → 关」能一次性解决，而那一项**没有对应的
    `settings` 键**，只能用手点一次。脚本会在需要时把路径打出来，并把「已了解
    应用的风险检测结果」与「继续安装」代点掉。
  * **vivo 的安装器提交会话后不关闭它**，因此 `pm install` 的退出**不能**当作
    「装完了」：实测包在 19:01:17 已经装好、启动器图标都出来了，命令仍然阻塞。
    据此判定会把成功报成失败。脚本改以设备侧事实为准（`dumpsys package` 的
    `lastUpdateTime`），`pm install` 的输出只在失败时用来取原因。

  同理，判断「当前是不是停在拦截页」也不能按 Activity 名认——
  `PackageInterceptActivity` 只出现在 `dumpsys window` 里，`uiautomator dump` 的
  XML 只有 `package="com.android.packageinstaller"` 和各控件文本；按名字认的症状
  是「界面明明停在拦截页，脚本却一直以为没有」，然后超时。

- **`docs/ANDROID.md` 新增第八节：真机部署与端到端复验。** 除了上面两条，还记下
  「没有公网节点怎么验 TUN 的 TCP 进不进内核」：用随包分发的
  `app/assets/bin/sing-box.exe` 在开发机上起一个 Shadowsocks 接入端，
  `adb reverse tcp:8388 tcp:8388` 把手机的端口反向映射回来，再用
  `ss://…@127.0.0.1:8388` 建一条配置——不走 WiFi，因此不受防火墙与同网段影响。
  判据取接入端日志里的 `inbound connection to <域名>:443`，**不取界面上的信号**：
  界面上的延迟与圆环在当初那个有缺陷的 `stack` 下同样是绿的，这正是误判的来源。

  同节附上这一轮复验的结论表：内置规则集 4 份 `.srs` + `cn-ip.bin` 全部解包、
  规则集大小四项都非 0、`credentials.key` 是 60 字节密文（12 IV + 32 DEK +
  16 GCM tag，无明文）、默认接口是 `wlan0 index=30` 而不是 `tun0`、
  网卡列表 3 张且不含 `tun0`、接入端收到隧道侧的 TCP。

- **新增 `test/scripts_syntax_test.dart`：把「脚本必须带 BOM 且能被解析」变成门禁。**
  这一轮里同一个坑踩了两次——`write` / `edit` 工具**不保留 UTF-8 BOM**，而
  PowerShell 5.1 按 ANSI 解码没有 BOM 的脚本，中文注释立刻变成乱码并报出
  「Try 语句缺少 Catch / Finally」这种与真实原因毫不相干的语法错误。改完必须
  手工补回 BOM，靠人记得不可靠。

  现在两条断言守住它：每个 `.ps1` 必须带 `EF BB BF`，并且（Windows 上）必须能被
  真正的 PowerShell 解析器读通。这两条都不是形式主义——把 BOM 去掉后**两条同时
  变红**，第二条正是第一条的真实后果。

  这也延续了 `updater_test.dart` 里那条判断：字符串断言只能证明「该有的片段在」，
  证明不了整份脚本能被解析。

## [1.2.1] - 2026-09-14

本版把「配置里写了、程序却没用」这一类静默失效集中收口，并修掉三处让 `Test` 门禁
变红/变抖的缺陷。1.2.0 的产物是在它的 tag 之前构建的，本版补上了紧随其后的
协议导入提示与托盘后台感知。

### 新增

- **协议导入提示**（`ProfileNotice`）：把「配置里写了、本客户端没让它生效」的字段
  从静默丢弃变成一条明确提示，并分两档——`warn` 是「不知道就连不上」，`info` 是
  「影响性能或稳定性」。导入确认表单、桌面配置卡、移动端列表共用同一份渲染
  （`ProfileNoticesView`），移动端把 `info` 收进「N 条提示」以免窄行被刷屏。
  已接入的判定：
  * WireGuard：AmneziaWG 混淆参数（内核不支持，必然连不上）；
  * WireGuard：`DNS` 只写了直连侧解析器（隧道内已改用公共解析器）；
  * WireGuard：未声明 `PersistentKeepalive`；
  * OpenVPN：`comp-lzo` / `fragment` / `http-proxy` 等**用户以为影响连通性**却未
    生效的指令（其余未识别指令只进「未使用字段」清单）；
  * OpenVPN：配置里有多条 `remote`（只采用第一条）；
  * OpenVPN：UDP 模式未声明 `ping` / `keepalive`；
  * Hysteria2：未声明 `up` / `down` 带宽（高丢包链路上自动探测常偏低）。

- **OpenVPN 隧道参数真正生效**：`tun-mtu`、`keepalive`（或 `ping` +
  `ping-restart`，含 `ping-restart 0` 表示的「禁用重启计时器」）、`mssfix`
  此前只被读进解析结果、从未写进内核端点——用户配了也不起作用，且看不出原因。
  现在它们进入 `openvpn-client` 端点，TUN 入站的 MTU 与端点取同一个值（避免系统栈
  组出装不进隧道的包），确认导入时会写回 `.ovpn`，并可在表单的「隧道参数（可选）」
  一栏编辑。

- **托盘后台感知**：关闭主窗口收进托盘时弹一次气泡 / 桌面通知（Windows
  `NIF_INFO`、Linux `GNotification`），文案按连接状态分两档——已连接时强调
  「隧道仍在后台保持连接」，未连接时只说明进程仍在运行，避免「后台运行」听起来
  像还在代理流量。只在「可见 → 隐藏」时弹一次，脚本反复触发关闭不会刷屏。托盘
  菜单补上连接状态项，并与版本项、「发现新版本」（可点）同序；已连接时把上下行
  速率附进 tooltip。Windows 侧同时打开 `NOTIFYICON_VERSION_4`。

### 改进

- **隧道内解析器不再复用直连偏好解析器**：`DNS = 223.5.5.5` 这类声明此前会被拿去
  解析海外域名——流量先出国再回问国内解析器，延迟高且易拿到错误的 CDN 节点，体感
  是「能连但海外站很卡」。现在隧道侧跳过这些地址（`isLocalPreferenceDns`），无可用
  地址才退回 `1.1.1.1`；直连侧与它同源，两处不再各写一份列表。
- **「本次连接」与「隧道 / 直连占比」分开讲清楚**：连接页第三张卡由「本次累计 +
  速率火花」改为「本次连接 + 隧道 / 直连拆分」（累计流量配速率曲线会误导），下行
  与上行火花分色；断开时「本次连接」与占比一并归零，否则重连后会把上一次的数字
  当成这一次的。关闭「记录分流明细」只停按域名记账，占比仍会更新——用户关掉明细
  不等于不要「这次连上有多少进了隧道」这个答案。
- **窄屏布局**：规则集、内置白名单、隧道流量去向等列表把「编辑 / 删除 / 改为直连」
  下移到次行右对齐，避免与长域名抢宽；密码框的显示 / 隐藏从文字链接改为眼睛图标
  （带 tooltip、热区 36×36），与其它表单控件同一观感。
- 措辞与文案收敛：界面与文档里「国内网段」改为「规则库网段」，「国内长尾站点补充」
  改为「直连站点补充」；推荐规则集不再转述上游的许可判断，只给地址与覆盖范围。
- 分流规则列表把「内置白名单」提到「手工指定」之上——底部输入区逻辑上对应手工规则，
  顺序对齐后不再看起来像白名单属于手工那一组。

### 修正

- **静态检查在 CI 上失败**：`core_monitor.dart` 里 `OutboundTags` 的导入在
  「隧道 / 直连占比」改为按连接观测后已无人使用，导入却仍在。
  `flutter analyze --fatal-infos` 因此报一条 warning，`Test` 门禁整条变红。
- **两处界面用例与发版撞车**：`update_card_test` / `update_center_test` 把假
  「新版本」号写死成 `1.2.0`，而卡片同时渲染「当前版本 v$appVersion」。版本号提到
  1.2.0 之后 `find.textContaining('1.2.0')` 会同时命中两个文本，`findsOneWidget`
  报「Found 2 widgets」。现在占位版本改用远离当前版本的 `9.9.9`，断言不再随发版
  而碎。
- **并发跑的测试会争同一对端口**：`PortAllocator` 是「探测到空闲就释放、内核随后
  再绑」，两步之间有时序窗口。多个测试文件同时起真实内核、又都从默认的 2080 起步
  时，可能在同一瞬间各自探到「2080 可用」，其中一个内核必然绑定失败——表现为随机
  失败的用例。现在 `SingBoxRunner` 的端口基准可注入（生产仍是 2080），并让并发
  起内核的测试文件各占一段。
- `SystemTray` 的类注释重复了一段（同一段「关窗收进托盘时的气泡」写了两遍）。
- `CHANGELOG` 缺 `[1.2.0]` 的引用链接定义，`## [1.2.0]` 在 Markdown 里渲染成断链。

### 文档

- `docs/PROTOCOLS.md` 新增 OpenVPN 的 `tun-mtu` / 保活 / `mssfix` 映射表，以及
  「隧道 DNS 卫生」一节；`docs/RESILIENCE.md` 与 `docs/USER_GUIDE.zh-CN.md`
  补上「关窗收进托盘会提示一次」的行为说明。

## [1.2.0] - 2026-09-14

### 文档

- **使用指南**：新增中英 `docs/USER_GUIDE`，覆盖自备/自建服务端 → 导入 → 连接 →
  分流 → 排障；服务端安装指向各协议官方文档，不提供节点或订阅。
- **法律与索引**：新增 `docs/LEGAL.md`、分轨 `docs/README.md`；根 README 收成门户，
  深内容下沉到 RULES / RESILIENCE / PROTOCOLS。
- **署名**：新增 `AUTHORS` / `.mailmap`；提交信息去掉助手 `Co-authored-by`；
  贡献指南禁止再写入工具共同作者。
- 收敛敏感表述；`RESEARCH.md` 只保留「为何选 sing-box」结论。

### 改进

- **连接页流量卡片**：区分当前速率、本次连接总量与隧道/直连占比；短连接首次出现
  即计入会话字节，避免 HTTP 短连接统计丢失。
- 断开后清零会话累计；托盘展示速率；下行/上行火花图配色区分。

## [1.1.0] - 2026-09-13

### 修正

这一轮修的是**分流证据的质量**。三处缺陷有共同的错误前提：原实现假定「判为直连
的主机要么通、要么不通」。而 2026-09 在深圳对 github.com 的实测表明存在第三种
状态——**握手成功但没有数据**（TLS 几百毫秒完成，随后连接活着 8–10 秒却只交付
0 字节）。围绕这个第三态，暴露出四个缺陷（第四个是写测试时被测试抓出来的）：

- **「成功」的定义里没有质量**。原先只要连接跑出过任何字节就算成功，于是零星
  几百字节被记成「直连能通」。现在只有字节数达到门槛（8 KB，来自实测：成功交付
  577 KB / 挂死交付 0 字节）才算**确实交付**；存活够久却几乎没交付的记为**挂死**，
  与连接失败同等对待，但单独计数以便界面区分「连不上」与「连上了没数据」。
- **成功会抹掉失败证据**。一次成功即清零连续失败计数、两次成功即撤销学到的规则。
  而实测 github.com 在坏时段里失败**成簇**、成功偶尔插进来——于是规则被反复推翻
  又重建。现在撤销要求**连续 3 次确实交付**，交替出现的成败永远攒不满阈值。
- **晋升条件里有一道永久闸门**。原先要求
  `consecutiveFailures >= 3 && directSuccesses == 0`：只要该域名历史上成功过
  **一次**，就再也学不会走隧道。对「偶尔能连上、大部分时候连不上」的域名
  （实测 github.com 正是这种形态）等于把学习路径整个关掉。现在只看连续计数——
  连续成功本身就会清零失败计数，那道闸门既多余又有害。
- **未定性的证据会立刻变成规则**（写测试时发现）。`AutoRouteEntry` 的默认
  `preference` 是 `forceProxy`，一次失败就把它装进表，而 `buildRouteRules()`
  会照它生成规则——于是文档里写的「连续 3 次才纠正」实际是「1 次就生效」，矛盾
  的方向还是**更激进**。现在未定性的证据攒在待定区，达到阈值才建规则；这与反方向
  纠正（国内解析→直连）一开始的做法保持一致。同时把观测按**时间窗**限流（同一
  域名同类观测 15 秒内只记一次）：整会话只记一次会让阈值永远攒不满，每条连接都记
  又会被一次页面加载的几十条连接刷爆。

另外，判定改在连接**关闭之后**做：一条连接刚出现时字节数还没有意义（新连接往往
只有首包），当时下结论会把「随后会挂住」误判成成功。这也让
`ClashConnection.startedAt`（此前被解析但从未使用）第一次真正派上用场。

实测数据、阈值来源与设计取舍见 [`docs/RULES.md`](docs/RULES.md) 的
「本地自动纠正表」一节。

- **新增的内置规则集对升级用户静默失效**（Windows 端到端自测发现）。规则的
  恢复语义是「存档里有 `ruleSets` 键就采用存档」，这样「用户删掉的内置规则集保持
  删除」。但那个语义对**新增**的内置规则集是错的：升级上来的用户存档里没有它，
  于是它永远不会出现。

  现象很有迷惑性：`geosite-cn-extra.srs` / `geoip-cn-extra.srs` **已经**被解包到
  磁盘，应用界面也会把它们显示成「未启用」——看起来像用户自己关的；而实际生成的
  内核配置里只引用了两个旧规则集（`rule_set=[geosite-cn,geoip-cn]`）。也就是本
  版本主打的「默认启用」在这些机器上完全没生效。

  修法是把「用户明确删除过什么」单独记下来（`removedBuiltins`），而不是靠
  「不在列表里」推断——后者无法区分「用户删了」与「这个规则集是新增的」。同时
  「恢复内置规则」会清空这份记录。

  验证方式是 Windows 端到端实跑：用得分流目标实测，`geoip-cn-extra` 覆盖的
  IP 字面量（`8.141.23.109`、`8.132.19.8`，裸 IP 无从嗅探域名）由修复前的走
  隧道变为直连；`geosite-cn-extra` 覆盖的 6 个长尾域名（均不在 `geosite-cn`
  也不在预置里）全部直连。

### 新增

- **决策热更新：改判不再需要重连**。此前内联路由规则写在 `config.json` 里，而内核
  拿着那份配置一直跑——**不重连就不读新规则**。于是程序刚学到的纠正、用户刚做的
  改判，在本次会话里统统不生效：界面已经提示「已自动纠正」，那个域名却照样打不开，
  必须手动重连。

  现在决策以**四份可热更新的规则集**投递：由应用进程在回环地址上提供，内核按
  `update_interval`（10 秒）反复拉取。四份而不是两份，是因为优先级位置不同——用户
  手工指定要早于 `ip_is_private`，程序学到与内置白名单要晚于它。DNS 规则引用同一批
  规则集，因此不会出现「路由已改判、DNS 仍按旧判断解析」。

  两种投递方式（内联、规则集）都从同一处派生（`AutoRouteTable.domainMatchForms()`），
  因此界面显示的走向与内核执行的走向不可能分叉。拿不到本地服务地址时自动退回内联
  规则，行为与改造前逐字节相同。

  设计被四条实测约束决定，全部记在 [`docs/RULES.md`](docs/RULES.md) 四点十三节：
  首次拉取失败会让内核**直接起不来**（因此服务必须先就绪并自检）、之后刷新失败不
  致命、remote 规则集没有本地缓存可退回（`path` 不是合法字段）、`file://` 不受支持。

  验证：`app/test/hot_route_sets_test.dart`（含用真实内核跑通「改判 → 运行中的内核
  跟着变」）；以及 Windows 端到端实测——学成规则后运行中的内核在 10 秒内改判，而
  **内核 PID 与配置写入时间都没有变化**（没有重连、没有重启、没有重新生成配置）。

- **交付速率判据**：补上「能通但很慢」这个缺口。此前的两种证据都是**二值**的
  （连接失败、握手成功但零字节），抓不住「交付了 448 KB 却用了 12 秒」——那在
  质量判定里算「确实交付」。

  判据是**相对**的：该域名的速率中位数低于**本session 所有实质连接的中位数**的
  1/4，连续 3 次即试走隧道。刻意不做「直连 vs 隧道」的跨时对比——实测
  github.com 在 40 分钟内从 40% 成功率变成 100%，两次测量不在同一条件下。

  两个方向且回滚条件不同：
  * 直连连续偏慢 → 学到走隧道，并标上「因速率而学到」；
  * **隧道同样偏慢** → 说明问题不在路径上，撤销、放回直连并进入 30 分钟冷却
    （避免「直连慢→上隧道→隧道也慢→回直连→又上隧道」）。

  那个标记是必需的：**因为直连失败**而学到的规则绝不能因为隧道也慢就放回直连。

  阈值与取舍见 [`docs/RULES.md`](docs/RULES.md) 的「交付速率」一节。其中
  「多慢算慢」（1/4）是本轮最需要现场校准的一个数，已如实标注。

### 变更

- **学习机制抽成独立模块**（`app/lib/core/route_learning.dart`）：策略、判据与
  决策集中到一处，`AutoRouteTable` 退回成「规则存储 + 匹配 + 历史证据 + 规则生成」。

  这次拆分的依据是「未来的迭代会碰什么」。会碰的只有三件事——一次观测算哪种证据、
  阈值、累计到多少才改判——而三件全是**纯逻辑**，没有一件需要碰路由表的存储、
  匹配或规则生成。因此把它们集中之后，迭代学习策略不必动那个承重的表。

  拆分前的具体问题是**策略常量横跨两个文件**：阈值在 `auto_route.dart`，判据与
  限流在 `core_monitor.dart`。后果是调一个阈值要先想清楚它在哪个文件；而新增挂死
  判据时，因为没有明确归属地，它就顺手落进了观测层。现在它们在且仅在一个文件里。

- **决策变成纯函数**：`judgeSetback` / `shouldRevokeLearnedProxy` /
  `LearningPolicy.classifyDirectConnection` 不持有状态、不碰路由表，因此可以在
  不构造路由表、不伪造网络的前提下逐条验证——新增的
  `app/test/route_learning_test.dart`（20 项）刻意不引入 `auto_route.dart`：
  若有人把策略又挪回表里，它会第一个失败。

- `LearningPolicy` 是策略的唯一事实来源；`AutoRouteTable` 的
  `promotionThreshold` / `capacity` / `decayAfter` 具名参数保留为**覆盖**，
  供测试与嵌入方使用（值仍落进策略对象，不另开一份来源）。

  **不变的**：阈值取值、判定语义、路由规则生成、存档格式。这次是行为保持的重构，
  920 项既有测试全数通过即为验证。

## [1.0.0] - 2026-09-13

首个公开版本。仓库自 2026-09-10 起开发，因此本节描述的是**当前真实落地的功能
集**，而不是相对某个更早版本的增量；所列内容均来自仓库历史与实现，未做前瞻性
承诺。

### 新增

**协议与配置**
- WireGuard（`.conf`）、OpenVPN（`.ovpn`）、Hysteria2（`.yaml` / `.yml`）：
  一律按**内容**识别，不靠扩展名——因此改名也能导入；每个协议有约定的后缀以便
  区分，但约定不是准入条件。分享链接与 sing-box 出站 JSON 同样可以解析。
- 参数规范化层：把加密套件名、摘要名、MTU 等归一化成内核认可的写法，认不出的
  直接剔除——这些参数写错会让内核**直接启动失败**，而错误信息只面向开发者。
- 导入路径：拖拽、系统文件选择器、粘贴文本；多份配置的记忆与切换。
- 配置与凭据的本地保护：Windows 用 DPAPI、Linux 优先用系统钥匙串
  （libsecret）；钥匙串不可用时降级为明文，并在界面**如实告知**。

**分流与 DNS**
- **「分流规则」页**：桌面侧栏（位于「设置」之上）与移动端底部标签各有一项，集中
  维护分流相关的一切，设置页不再承载这些内容。
  - **规则集**：四个内置规则集（`geosite-cn`、`geoip-cn` 与两份国内补充）可
    启用/停用/删除/新增自定义，并可「恢复内置规则」；自定义规则集会被真正写进
    内核配置（禁用或删除的不会出现）。
  - **域名分流规则**：分「默认规则」（不可删除）、「程序学到」与「手工指定」三组，
    可新增/编辑/删除；自动纠正学到的规则可逐条撤销。
  - **分流模式**（智能分流 / 全局代理 / 全局直连）也移到这里。
  - 「恢复内置规则」的作用域是：重新启用两个内置规则集，并清除**程序学到**的规则，
    **保留手工指定**的规则与自定义规则集；执行前会明确说明。
- 规则集随包分发，首次连接解包到应用私有目录，可「检查更新」增量刷新。
- 双解析器 DNS：命中规则集的域名走直连解析器，其余在隧道内解析，避免不一致的
  答案驱动分流。
- DNS 交叉校验：同一域名分别用直连解析器与内核 `/dns/query` 解析，比较答案集
  并用 `geoip-cn` 前缀索引判断归属，区分「双部署」与「答案不一致」。
- 自动纠正：连续「判为直连却失败」的域名会被改走隧道，并以最高优先级注入路由
  规则；一次直连成功即清零失败计数，两次成功则撤销——反证优先于猜测。
- **内置国内域名补充规则集**（`geosite-cn-extra`，默认启用）：由
  `scripts/build-cn-domain-ruleset.ps1` 从 `felixonmars/dnsmasq-china-list`
  （WTFPL v2）编译，110454 条域名后缀。默认启用是**实测**结论——召回 40/40
  （100%），误命中约 150 个境外域名（含直连常失败样本类别）**0 个**；且万一有域名被
  错列，它被判直连后连接失败会被自动纠正表学成走隧道，因此这类错误可自愈。
- **内置国内 IP 补充规则集**（`geoip-cn-extra`，默认启用）：由
  `scripts/build-cn-ip-ruleset.ps1` 从 `gaoyifan/china-operator-ip`（MIT）编译，
  补上 `geoip-cn` **整块缺失**的 `8.0.0.0/8`（含阿里云国内段）。两份列表是
  **并集**而非替换——实测各有对方没有的网段。
- **隧道流量去向**：「分流规则」页新增清单，把已观测到的走隧道目标按流量降序
  排出，标注每条「为什么在隧道里」（命中规则库 / 有专门规则 / 默认规则兜底），
  并提供就地「改为直连」。它补的是一个此前完全不可见的缺口：**误入隧道的流量
  不失败、不报错**，只白占隧道带宽，用户看不出原因。
- **反方向自动纠正**：原先只会往隧道里推流量（依据「判为直连却失败」），现在
  也会把**本该直连却走了隧道**的域名拉出来——依据是 DNS 交叉校验给出的**事实**
  （该域名的直连解析落在国内网段内），连续 2 次独立测量后改为直连。与正向纠正
  共用同一张表与同一套撤销/衰减规则，任何一次直连失败都会清零计数并改回走隧道。
- **直连白名单**：把「确实能直连、却被默认规则送进隧道」的域名显式列出，两类：
  国内长尾站点补充（默认启用）与境外应用预置（如 Cursor，按需启用）。两者都不
  依赖解析结果，因此不受不可信直连 DNS 答案的影响。
- **「推荐规则集」区块**：把覆盖更全但许可有灰区的第三方规则集以「只给地址、
  不再分发」的方式提供给用户自行添加——不再分发数据，也就不把上游的许可义务
  带进本项目的产物。
- **DNS 决策跟随路由决策**：此前 `dns.rules` 只认一个规则集，而路由决策来自
  多个来源，两者会互相矛盾（已判定该直连的域名仍被境外解析器解析，于是拿到
  境外 CDN 地址再去直连）。现在 DNS 与路由从**同一个**决策表派生，且全部
  域名类规则集一起参与。
- **分流优先级是一份写进代码的契约**：用户手工指定 → 内网地址直连 →
  程序学到/内置白名单 → 规则库 → 兜底走隧道。有测试直接断言这五个位置的下标
  关系，因此重排会被拦下，而不是等用户发现某类站点坏了。

**诊断与观测**
- 失败归因区分三类：直连失败（本地网络 / DNS）、隧道失败（节点 / 服务器）、
  规则覆盖缺口；启动自检分别验证两条腿。
- 连接列表、按出站的字节统计与实时速率来自内核 Clash API（仅回环 `127.0.0.1`）。
- 自动恢复：内核崩溃 / 隧道不通 / 进程卡住时带次数与冷却限制地重启；
  2080 / 2081 端口被占用时自动换端口。
- **自检四条腿**：直连、隧道、直连解析、**隧道解析**。最后一条此前是盲区——
  旧的 DNS 探针用的是命中规则集的域名，因而走直连解析器，从未验证过隧道解析器，
  而后者是所有境外站点的解析出口。现在它单独成一条探针，失败时给出
  「隧道不通 DNS」的结论（多半是节点不允许 UDP/53 出站）。
- **可复核的测量脚本**：`scripts/measure-ruleset-runtime-cost.ps1`（交替采样的
  规则集运行时开销）与 `scripts/review-cn-ruleset-quality.ps1`（固定种子的清单
  组成抽样）。两者都只用随包内核 + 本地死端口出站，不产生到任何真实服务器的流量。

**平台与界面**
- **Windows** 桌面端：无边框自绘标题栏、系统托盘、系统代理接管与还原
  （免管理员权限）。
- **Android** 端：`VpnService` 提供 TUN，`libbox` 作为进程内原生库。
- **Linux** 桌面端：与 Windows 相同的「子进程内核 + 系统代理」模型，无需 root；
  桌面环境支持 GNOME 系（gsettings）与 KDE；X11 下自绘标题栏、Wayland 下回退
  系统装饰。
- **系统托盘**（Windows 与 Linux）：显示版本号与连接状态，未连接时图标置灰，有可用
  更新时提示新版本——该提示**可点**，点它会显示主界面并跳到更新入口；关闭主窗口
  收进托盘、连接不中断。Linux 走 StatusNotifierItem（libayatana-appindicator3，
  运行期 `dlopen`，装不到就降级为无托盘），且托盘退出与 `SIGTERM` 都先由 Dart
  还原系统代理、结束内核后再退出。
- 一套状态层、两种布局：以 900px 为界切换桌面侧栏与移动底部标签；两端呈现结构
  由 `app/test/platform_parity_test.dart` 的断言守住。

**应用内更新**
- 启动时**静默检查一次**更新：每次启动在后台查询一次 GitHub Releases，仅在发现
  新版本时提示。桌面端在标题栏出现一个可点击的提示入口，移动端在设置页「版本
  更新」卡片呈现——卡片直接显示启动检查发现的版本，无需用户先点「检查更新」。
  不会自动下载、不会弹窗打断；检查失败与「没有新版本」在体验上完全一致。
- 手动「检查更新」→ 下载（带进度、可取消）→ **校验 `SHA256SUMS.txt` 通过后**
  才安装；桌面端在用户确认后自替换并重启，Android 交由系统安装器。提示可「忽略」，
  忽略后本次运行不再打扰。

### 修正

以下各项都在**开发期**被测试或实测拦下，随首个公开版本一并修复。留此记录，是因为
其中几处属于同一类问题：**界面承诺了，代码没兑现**，而且都不报错。

- **托盘「发现新版本」不能点**：该菜单项被做成灰色纯信息项（Windows `MF_GRAYED`、
  Linux `set_sensitive(FALSE)`），应用告诉用户有新版本却不给他任何出路。它当初的
  理由「没有一条现成的 native → Dart 通道」**是不成立的**——同一条通道本就在用
  （`maximizedChanged`、`quitRequested`）。现在两端都可点，点它显示主界面并跳到
  更新入口。顺带做了一次**三端通道对账**（逐条核对原生推送与 Dart 处理器），
  确认没有第二处未配对的推送。
- **`.cn` 域名曾整批缺失**：上游**故意不列举** `.cn`，靠 dnsmasq 的
  `server=/cn/` 顶级规则表达（110460 个条目里含 `.cn` 的为 0）。构建脚本把单标签
  条目当「不是域名」跳过，于是产物系统性缺失整批 `.cn`。症状极具迷惑性：40 个
  国内站点里漏掉的 6 个**全是 `.cn`**，而它们能命中 `geosite-cn`，寻常检查看不出来。
  修法是保留单标签条目（与 dnsmasq 语义等价，已用内核验证标签边界），
  召回率 **85% → 100%**；脚本自检现在显式断言顶级规则存在、标签边界正确、
  境外域名不命中。
- **内网地址可能被「学到」的规则推进隧道**：`ip_is_private` 原先排在整张决策表
  **之后**，于是程序学到或内置白名单的强制代理能覆盖它。内网主机名恰好会落进
  这两类证据（`nas.local` 临时不可达就会攒够「直连失败」），结果是内网流量进隧道，
  既费流量又必然连不上。现在按五段固定优先级排列，并由测试断言下标关系。
- **推荐规则集加了却不参与 DNS 分流**：「是否域名类」先是硬编码 `tag ==
  'geosite-cn'`，改成由内置定义声明后**自定义条目仍一律按非域名类**处理——而
  「推荐规则集」走的正是自定义路径。于是按我们自己的推荐添加进来的规则集，被它
  判为直连的域名仍经隧道解析。现在标记随条目走并持久化进存档。
- **出站标签在 5 处被重复写成字面量**（`OutboundTags`）：风险不对称且隐蔽——改了
  标签会让路由照常工作，而所有按出站判定的统计**静默失效**（界面显示「走隧道 0 B」
  而隧道其实在工作）。已收敛为单一来源。
- **流量增量只按目标归并**：同一个目标在同一秒内既有直连又有代理连接时，两路字节
  会被合并记到「最后一条连接」的方向上。现在按 `(目标, 走向)` 归并。
- **构建期产物被当成可下载的规则集**：`RuleSetEntry` 新增 `updatable`；此前用户点
  「检查更新」会拿到一份配置文本、被魔数校验拒绝，而报错指向他的网络。
- **存档兼容性两处回退**：`updatable` 缺失默认 `true`；`domainRuleSet` 缺失时
  **回退到内置定义**（若默认 `false`，升级用户会静默失去「命中规则集的域名走
  直连解析器」）。
- **文档纠正一处被实测否证的结论**：原先写「域名未命中 `geosite-cn` 时，内核会先
  解析域名再拿结果匹配 `geoip-cn`，因此长尾站点仍会走直连」。用随包内核做的受控
  实验否证了这一点——**判定过程中不会发生任何域名解析**，`geoip-cn` 只对**目标
  本身就是 IP** 的连接生效。也就是说「域名与 IP 双保险」对域名流量并不成立。
  方法与数据见 [`docs/RULES.md`](docs/RULES.md)。
- 「恢复内置规则」的说明补齐：明确列出不会被改动的三项（手工规则、自定义与推荐
  规则集、直连白名单开关）。

### 安全与合规

- 采用 **GPL-3.0-or-later**：受内置内核
  [sing-box](https://github.com/SagerNet/sing-box)（GPL-3.0-or-later，`v1.14.0`）
  约束——Android 端以原生库形式把它链接进同一进程，构成 GPL 意义上的组合作品。
- 第三方声明见 [`NOTICE.md`](NOTICE.md)：sing-box（含上游「不得暗示关联」附加
  条款与对应源码获取方式）、GPL-3.0-or-later 的规则库、Dart/Flutter 依赖、
  字体图标、出口管制提示；内核静态内嵌的 Go 依赖许可由
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) 聚合列出（sing-box `v1.14.0`
  下 111 个模块，含许可标识与全文）。
- 发布产物**随附许可证**：Windows / Linux 压缩包根含 `LICENSE`、`NOTICE.md` 与
  `THIRD-PARTY-NOTICES.md`，Android APK 内含 `assets/licenses/` 下的同名文件。
- 应用内**设置页「关于 → 开源许可」**可读上述三份文本与 Flutter 生成的
  `NOTICES.Z`，无需离开应用。
- 隐私政策 [`PRIVACY.md`](PRIVACY.md) 逐项列出本地数据与应用会发起的全部网络
  连接；应用不集成任何分析、广告或崩溃上报 SDK，也不运营后端。
- 新增 [`SECURITY.md`](SECURITY.md)（私密漏洞报告渠道）、本文件与
  [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。

### 已知限制

- 桌面端只接管**认系统代理**的程序：浏览器与多数桌面软件生效，游戏与命令行
  工具不生效。桌面端 TUN 需要 wintun 驱动或提权辅助进程，当前版本未内置，
  设置页**刻意不提供**该选项，而不是给一个拨了没反应的开关。
- 尚无 iOS 工程。

[1.2.0]: https://github.com/LSD-Apps/XVPN/releases/tag/v1.2.0
[1.1.0]: https://github.com/LSD-Apps/XVPN/releases/tag/v1.1.0
[1.0.0]: https://github.com/LSD-Apps/XVPN/releases/tag/v1.0.0
