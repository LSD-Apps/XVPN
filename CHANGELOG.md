# 更新日志 · Changelog

本文件记录 XVPN 的每个公开版本。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

> 版本号的**唯一来源**是 [`app/pubspec.yaml`](app/pubspec.yaml) 的 `version:`
> （去掉 `+build` 后缀）。发布 tag 必须与它一致，否则发布流水线会直接失败——
> 见 [`.github/workflows/release.yml`](.github/workflows/release.yml)。

## [未发布]

### 修正

这一轮修的是**分流证据的质量**。三处缺陷有共同的错误前提：原实现假定「判为直连
的主机要么通、要么不通」。而 2026-09 在深圳对 github.com 的实测表明存在第三种
状态——**握手成功但没有数据**（TLS 几百毫秒完成，随后连接活着 8–10 秒却只交付
0 字节）。围绕这个第三态，暴露出四个缺陷（第四个是写测试时被测试抓出来的）：

- **「成功」的定义里没有质量**。原先只要连接跑出过任何字节就算成功，于是零星
  几百字节被记成「直连能通」。现在只有字节数达到门槛（8 KB，来自实测：成功交付
  577 KB / 挂死交付 0 字节）才算**确实交付**；存活够久却几乎没交付的记为**挂死**，
  与连接失败同等对待，但单独计数以便界面区分「连不上」与「连上了没数据」。
- **成功会抹掉失败证据**。一次成功即清零连续失败计数、两次成功即撤销学到的规则。
  而实测 github.com 在坏时段里失败**成簇**、成功偶尔插进来——于是规则被反复推翻
  又重建。现在撤销要求**连续 3 次确实交付**，交替出现的成败永远攒不满阈值。
- **晋升条件里有一道永久闸门**。原先要求
  `consecutiveFailures >= 3 && directSuccesses == 0`：只要该域名历史上成功过
  **一次**，就再也学不会走隧道。对「偶尔能连上、大部分时候连不上」的域名
  （实测 github.com 正是这种形态）等于把学习路径整个关掉。现在只看连续计数——
  连续成功本身就会清零失败计数，那道闸门既多余又有害。
- **未定性的证据会立刻变成规则**（写测试时发现）。`AutoRouteEntry` 的默认
  `preference` 是 `forceProxy`，一次失败就把它装进表，而 `buildRouteRules()`
  会照它生成规则——于是文档里写的「连续 3 次才纠正」实际是「1 次就生效」，矛盾
  的方向还是**更激进**。现在未定性的证据攒在待定区，达到阈值才建规则；这与反方向
  纠正（国内解析→直连）一开始的做法保持一致。同时把观测按**时间窗**限流（同一
  域名同类观测 15 秒内只记一次）：整会话只记一次会让阈值永远攒不满，每条连接都记
  又会被一次页面加载的几十条连接刷爆。

另外，判定改在连接**关闭之后**做：一条连接刚出现时字节数还没有意义（新连接往往
只有首包），当时下结论会把「随后会挂住」误判成成功。这也让
`ClashConnection.startedAt`（此前被解析但从未使用）第一次真正派上用场。

实测数据、阈值来源与设计取舍见 [`docs/RULES.md`](docs/RULES.md) 的
「本地自动纠正表」一节。

## [1.0.0] - 2026-09-13

首个公开版本。仓库自 2026-09-10 起开发，因此本节描述的是**当前真实落地的功能
集**，而不是相对某个更早版本的增量；所列内容均来自仓库历史与实现，未做前瞻性
承诺。

### 新增

**协议与配置**
- WireGuard（`.conf`）、OpenVPN（`.ovpn`）、Hysteria2（`.yaml` / `.yml`）：
  一律按**内容**识别，不靠扩展名——因此改名也能导入；每个协议有约定的后缀以便
  区分，但约定不是准入条件。分享链接与 sing-box 出站 JSON 同样可以解析。
- 参数规范化层：把加密套件名、摘要名、MTU 等归一化成内核认可的写法，认不出的
  直接剔除——这些参数写错会让内核**直接启动失败**，而错误信息只面向开发者。
- 导入路径：拖拽、系统文件选择器、粘贴文本；多份配置的记忆与切换。
- 配置与凭据的本地保护：Windows 用 DPAPI、Linux 优先用系统钥匙串
  （libsecret）；钥匙串不可用时降级为明文，并在界面**如实告知**。

**分流与 DNS**
- **「分流规则」页**：桌面侧栏（位于「设置」之上）与移动端底部标签各有一项，集中
  维护分流相关的一切，设置页不再承载这些内容。
  - **规则集**：四个内置规则集（`geosite-cn`、`geoip-cn` 与两份国内补充）可
    启用/停用/删除/新增自定义，并可「恢复内置规则」；自定义规则集会被真正写进
    内核配置（禁用或删除的不会出现）。
  - **域名分流规则**：分「默认规则」（不可删除）、「程序学到」与「手工指定」三组，
    可新增/编辑/删除；自动纠正学到的规则可逐条撤销。
  - **分流模式**（智能分流 / 全局代理 / 全局直连）也移到这里。
  - 「恢复内置规则」的作用域是：重新启用两个内置规则集，并清除**程序学到**的规则，
    **保留手工指定**的规则与自定义规则集；执行前会明确说明。
- 规则集随包分发，首次连接解包到应用私有目录，可「检查更新」增量刷新。
- 双解析器 DNS：命中规则集的域名走直连解析器，其余在隧道内解析，避免不一致的
  答案驱动分流。
- DNS 交叉校验：同一域名分别用直连解析器与内核 `/dns/query` 解析，比较答案集
  并用 `geoip-cn` 前缀索引判断归属，区分「双部署」与「答案不一致」。
- 自动纠正：连续「判为直连却失败」的域名会被改走隧道，并以最高优先级注入路由
  规则；一次直连成功即清零失败计数，两次成功则撤销——反证优先于猜测。
- **内置国内域名补充规则集**（`geosite-cn-extra`，默认启用）：由
  `scripts/build-cn-domain-ruleset.ps1` 从 `felixonmars/dnsmasq-china-list`
  （WTFPL v2）编译，110454 条域名后缀。默认启用是**实测**结论——召回 40/40
  （100%），误命中约 150 个境外域名（含被墙服务类别）**0 个**；且万一有域名被
  错列，它被判直连后连接失败会被自动纠正表学成走隧道，因此这类错误可自愈。
- **内置国内 IP 补充规则集**（`geoip-cn-extra`，默认启用）：由
  `scripts/build-cn-ip-ruleset.ps1` 从 `gaoyifan/china-operator-ip`（MIT）编译，
  补上 `geoip-cn` **整块缺失**的 `8.0.0.0/8`（含阿里云国内段）。两份列表是
  **并集**而非替换——实测各有对方没有的网段。
- **隧道流量去向**：「分流规则」页新增清单，把已观测到的走隧道目标按流量降序
  排出，标注每条「为什么在隧道里」（命中规则库 / 有专门规则 / 默认规则兜底），
  并提供就地「改为直连」。它补的是一个此前完全不可见的缺口：**误入隧道的流量
  不失败、不报错**，只白占隧道带宽，用户看不出原因。
- **反方向自动纠正**：原先只会往隧道里推流量（依据「判为直连却失败」），现在
  也会把**本该直连却走了隧道**的域名拉出来——依据是 DNS 交叉校验给出的**事实**
  （该域名的直连解析落在国内网段内），连续 2 次独立测量后改为直连。与正向纠正
  共用同一张表与同一套撤销/衰减规则，任何一次直连失败都会清零计数并改回走隧道。
- **直连白名单**：把「确实能直连、却被默认规则送进隧道」的域名显式列出，两类：
  国内长尾站点补充（默认启用）与境外应用预置（如 Cursor，按需启用）。两者都不
  依赖解析结果，因此不受 DNS 污染影响。
- **「推荐规则集」区块**：把覆盖更全但许可有灰区的第三方规则集以「只给地址、
  不再分发」的方式提供给用户自行添加——不再分发数据，也就不把上游的许可义务
  带进本项目的产物。
- **DNS 决策跟随路由决策**：此前 `dns.rules` 只认一个规则集，而路由决策来自
  多个来源，两者会互相矛盾（已判定该直连的域名仍被境外解析器解析，于是拿到
  境外 CDN 地址再去直连）。现在 DNS 与路由从**同一个**决策表派生，且全部
  域名类规则集一起参与。
- **分流优先级是一份写进代码的契约**：用户手工指定 → 内网地址直连 →
  程序学到/内置白名单 → 规则库 → 兜底走隧道。有测试直接断言这五个位置的下标
  关系，因此重排会被拦下，而不是等用户发现某类站点坏了。

**诊断与观测**
- 失败归因区分三类：直连失败（本地网络 / DNS）、隧道失败（节点 / 服务器）、
  规则覆盖缺口；启动自检分别验证两条腿。
- 连接列表、按出站的字节统计与实时速率来自内核 Clash API（仅回环 `127.0.0.1`）。
- 自动恢复：内核崩溃 / 隧道不通 / 进程卡住时带次数与冷却限制地重启；
  2080 / 2081 端口被占用时自动换端口。
- **自检四条腿**：直连、隧道、直连解析、**隧道解析**。最后一条此前是盲区——
  旧的 DNS 探针用的是命中规则集的域名，因而走直连解析器，从未验证过隧道解析器，
  而后者是所有境外站点的解析出口。现在它单独成一条探针，失败时给出
  「隧道不通 DNS」的结论（多半是节点不允许 UDP/53 出站）。
- **可复核的测量脚本**：`scripts/measure-ruleset-runtime-cost.ps1`（交替采样的
  规则集运行时开销）与 `scripts/review-cn-ruleset-quality.ps1`（固定种子的清单
  组成抽样）。两者都只用随包内核 + 本地死端口出站，不产生到任何真实服务器的流量。

**平台与界面**
- **Windows** 桌面端：无边框自绘标题栏、系统托盘、系统代理接管与还原
  （免管理员权限）。
- **Android** 端：`VpnService` 提供 TUN，`libbox` 作为进程内原生库。
- **Linux** 桌面端：与 Windows 相同的「子进程内核 + 系统代理」模型，无需 root；
  桌面环境支持 GNOME 系（gsettings）与 KDE；X11 下自绘标题栏、Wayland 下回退
  系统装饰。
- **系统托盘**（Windows 与 Linux）：显示版本号与连接状态，未连接时图标置灰，有可用
  更新时提示新版本——该提示**可点**，点它会显示主界面并跳到更新入口；关闭主窗口
  收进托盘、连接不中断。Linux 走 StatusNotifierItem（libayatana-appindicator3，
  运行期 `dlopen`，装不到就降级为无托盘），且托盘退出与 `SIGTERM` 都先由 Dart
  还原系统代理、结束内核后再退出。
- 一套状态层、两种布局：以 900px 为界切换桌面侧栏与移动底部标签；两端呈现结构
  由 `app/test/platform_parity_test.dart` 的断言守住。

**应用内更新**
- 启动时**静默检查一次**更新：每次启动在后台查询一次 GitHub Releases，仅在发现
  新版本时提示。桌面端在标题栏出现一个可点击的提示入口，移动端在设置页「版本
  更新」卡片呈现——卡片直接显示启动检查发现的版本，无需用户先点「检查更新」。
  不会自动下载、不会弹窗打断；检查失败与「没有新版本」在体验上完全一致。
- 手动「检查更新」→ 下载（带进度、可取消）→ **校验 `SHA256SUMS.txt` 通过后**
  才安装；桌面端在用户确认后自替换并重启，Android 交由系统安装器。提示可「忽略」，
  忽略后本次运行不再打扰。

### 修正

以下各项都在**开发期**被测试或实测拦下，随首个公开版本一并修复。留此记录，是因为
其中几处属于同一类问题：**界面承诺了，代码没兑现**，而且都不报错。

- **托盘「发现新版本」不能点**：该菜单项被做成灰色纯信息项（Windows `MF_GRAYED`、
  Linux `set_sensitive(FALSE)`），应用告诉用户有新版本却不给他任何出路。它当初的
  理由「没有一条现成的 native → Dart 通道」**是不成立的**——同一条通道本就在用
  （`maximizedChanged`、`quitRequested`）。现在两端都可点，点它显示主界面并跳到
  更新入口。顺带做了一次**三端通道对账**（逐条核对原生推送与 Dart 处理器），
  确认没有第二处未配对的推送。
- **`.cn` 域名曾整批缺失**：上游**故意不列举** `.cn`，靠 dnsmasq 的
  `server=/cn/` 顶级规则表达（110460 个条目里含 `.cn` 的为 0）。构建脚本把单标签
  条目当「不是域名」跳过，于是产物系统性缺失整批 `.cn`。症状极具迷惑性：40 个
  国内站点里漏掉的 6 个**全是 `.cn`**，而它们能命中 `geosite-cn`，寻常检查看不出来。
  修法是保留单标签条目（与 dnsmasq 语义等价，已用内核验证标签边界），
  召回率 **85% → 100%**；脚本自检现在显式断言顶级规则存在、标签边界正确、
  境外域名不命中。
- **内网地址可能被「学到」的规则推进隧道**：`ip_is_private` 原先排在整张决策表
  **之后**，于是程序学到或内置白名单的强制代理能覆盖它。内网主机名恰好会落进
  这两类证据（`nas.local` 临时不可达就会攒够「直连失败」），结果是内网流量进隧道，
  既费流量又必然连不上。现在按五段固定优先级排列，并由测试断言下标关系。
- **推荐规则集加了却不参与 DNS 分流**：「是否域名类」先是硬编码 `tag ==
  'geosite-cn'`，改成由内置定义声明后**自定义条目仍一律按非域名类**处理——而
  「推荐规则集」走的正是自定义路径。于是按我们自己的推荐添加进来的规则集，被它
  判为直连的域名仍经隧道解析。现在标记随条目走并持久化进存档。
- **出站标签在 5 处被重复写成字面量**（`OutboundTags`）：风险不对称且隐蔽——改了
  标签会让路由照常工作，而所有按出站判定的统计**静默失效**（界面显示「走隧道 0 B」
  而隧道其实在工作）。已收敛为单一来源。
- **流量增量只按目标归并**：同一个目标在同一秒内既有直连又有代理连接时，两路字节
  会被合并记到「最后一条连接」的方向上。现在按 `(目标, 走向)` 归并。
- **构建期产物被当成可下载的规则集**：`RuleSetEntry` 新增 `updatable`；此前用户点
  「检查更新」会拿到一份配置文本、被魔数校验拒绝，而报错指向他的网络。
- **存档兼容性两处回退**：`updatable` 缺失默认 `true`；`domainRuleSet` 缺失时
  **回退到内置定义**（若默认 `false`，升级用户会静默失去「命中规则集的域名走
  直连解析器」）。
- **文档纠正一处被实测否证的结论**：原先写「域名未命中 `geosite-cn` 时，内核会先
  解析域名再拿结果匹配 `geoip-cn`，因此长尾站点仍会走直连」。用随包内核做的受控
  实验否证了这一点——**判定过程中不会发生任何域名解析**，`geoip-cn` 只对**目标
  本身就是 IP** 的连接生效。也就是说「域名与 IP 双保险」对域名流量并不成立。
  方法与数据见 [`docs/RULES.md`](docs/RULES.md)。
- 「恢复内置规则」的说明补齐：明确列出不会被改动的三项（手工规则、自定义与推荐
  规则集、直连白名单开关）。

### 安全与合规

- 采用 **GPL-3.0-or-later**：受内置内核
  [sing-box](https://github.com/SagerNet/sing-box)（GPL-3.0-or-later，`v1.14.0`）
  约束——Android 端以原生库形式把它链接进同一进程，构成 GPL 意义上的组合作品。
- 第三方声明见 [`NOTICE.md`](NOTICE.md)：sing-box（含上游「不得暗示关联」附加
  条款与对应源码获取方式）、GPL-3.0-or-later 的规则库、Dart/Flutter 依赖、
  字体图标、出口管制提示；内核静态内嵌的 Go 依赖许可由
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) 聚合列出（sing-box `v1.14.0`
  下 111 个模块，含许可标识与全文）。
- 发布产物**随附许可证**：Windows / Linux 压缩包根含 `LICENSE`、`NOTICE.md` 与
  `THIRD-PARTY-NOTICES.md`，Android APK 内含 `assets/licenses/` 下的同名文件。
- 应用内**设置页「关于 → 开源许可」**可读上述三份文本与 Flutter 生成的
  `NOTICES.Z`，无需离开应用。
- 隐私政策 [`PRIVACY.md`](PRIVACY.md) 逐项列出本地数据与应用会发起的全部网络
  连接；应用不集成任何分析、广告或崩溃上报 SDK，也不运营后端。
- 新增 [`SECURITY.md`](SECURITY.md)（私密漏洞报告渠道）、本文件与
  [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。

### 已知限制

- 桌面端只接管**认系统代理**的程序：浏览器与多数桌面软件生效，游戏与命令行
  工具不生效。桌面端 TUN 需要 wintun 驱动或提权辅助进程，当前版本未内置，
  设置页**刻意不提供**该选项，而不是给一个拨了没反应的开关。
- 尚无 iOS 工程。

[1.0.0]: https://github.com/LSD-Apps/XVPN/releases/tag/v1.0.0
